# Registro de cambios fuera del plan original

## Propósito y regla de mantenimiento

Este documento es el registro normativo de decisiones que amplían, corrigen o
aclaran el plan original del orquestador. Evita que las condiciones reales del
nodo, los compromisos de despliegue y los contratos ya implementados queden
dispersos entre conversaciones, commits y ejemplos.

**Regla desde 2026-07-13:** antes de implementar un cambio que no esté descrito
en la Guía Técnica, el Checklist o el Plan de Tests, se añadirá aquí una entrada
con motivación, impacto, módulos afectados, estado y pruebas necesarias. Este
registro no sustituye los documentos base: los complementa y prevalece cuando
describe una adaptación explícita de plataforma.

## Estado de referencia

- Plan base: `Guia_Tecnica_Orquestador_Campanas.md`,
  `Checklist_Validaciones_Tecnicas_Orquestador.md` y
  `Plan_Tests_Orquestador.md`.
- Plataforma operativa actual confirmada: SC3, nodo `felix.sc3.uis.edu.co`.
- `smexa` y `exadell` están descartados para las pruebas actuales porque no
  cumplen el requisito de GPU NVIDIA. Sus diagnósticos se conservan únicamente
  como evidencia comparativa de infraestructura.
- Entorno de ejecución: Slurm + cgroup v2 + Conda con Python 3.11+.
- Política de seguridad: la detección y el diagnóstico son de solo lectura;
  ningún diagnóstico escribe sysfs ni requiere root.

## Handoff para el modelo que mantiene los documentos base

Este apartado permite reconciliar la documentación original con el código y la
infraestructura reales. Leerlo antes de modificar la Guía, Checklist, Plan de
Tests o de proponer módulos nuevos.

### Estado del proyecto y fuente de verdad

1. El plan original sigue definiendo la arquitectura final, pero **no describe
   completamente las restricciones observadas en SC3**. Las enmiendas ARC de
   este archivo son obligatorias para cualquier revisión del plan.
2. **Actualizado 2026-08-01:** todos los módulos del orquestador tienen
   contrato implementado y pruebas unitarias (`catalog.py`, `config.py`,
   `environment.py`, `manifest.py`, `preflight.py`, `node_profile.py`,
   `diagnostics.py`, `freqctl.py`, `calibration.py`, `runner.py`,
   `campaign.py`, `metadata_schema.py`, `postprocess.py`, `validation.py`,
   `report.py`, `cli.py`). Auditoría de checklist §12.1-12.4/12.12-12.13
   reconciliada contra código real ese mismo día (todas las reglas MAN/ENV/
   PRE-E/PRE-I/PRE-C/PRE-D/PRE-OPS/PRE-G/MLT/CPP pasaron a ☑ salvo ENV-12
   (`gpu_vendor`) y MLT-06 (commit hash en metadata), confirmados ausentes
   — ver Guía Maestra. Lo que SÍ sigue sin ejecutarse contra hardware real
   es una campaña completa de punta a punta (`campaign.run_campaign()` con
   kernels reales, F4.4) y la prueba de caos de `freqctl.py` (H3, FRQ-08).
3. La rama de despliegue es `hpc-startup-diagnostic`. Hitos relevantes:
   `9dcd733` añadió diagnóstico/Conda/plantilla SC3, `dc1047f` adaptó
   environment/preflight a las capacidades reales, y el 2026-08-01
   `environment.detect_environment()` y `preflight.run_campaign_preflight()`
   corrieron contra felix por primera vez (F4.2, ARC-36/ARC-37): 42/42
   checks en verde con el catálogo real de 8 kernels. La suite local llegó
   a 176 pruebas aprobadas; sigue pendiente la prueba de caos de hardware
   (H3/FRQ-08) y la campaña piloto completa (F4.4).
4. `startup_diagnostic.json` confirma carga estructural de manifest y catálogo,
   topología, RAPL, perf y cpuset. No reemplaza C01/C02, preflight de campaña,
   calibración ni una corrida del harness. (El preflight real de F4.2 sí
   reemplaza esa verificación — usar `environment_report.json`/
   `node_profile.json` de esa corrida como referencia, no `startup_diagnostic.json`,
   que quedó desactualizado desde julio.)
5. Para conflictos entre documentación y nodo real, prevalecen: (a) el cpuset
   efectivo del job, (b) `EnvironmentProfile`, (c) el preflight y (d) este
   registro. No inferir permisos ni control DVFS desde el modelo de CPU.

### Contratos vigentes que la documentación debe preservar

- `environment.py` es la única autoridad de detección. Otros módulos consumen
  su perfil; no reimplementan sondeos sysfs.
- `freq_control_capable` indica soporte de driver/niveles, mientras
  `frequency_write_capable` indica autoridad efectiva del usuario. Ambos son
  necesarios para producir datos de entrenamiento DVFS.
- `frequency_control_strategy` puede ser `discrete_bounds`, `bounded_range` o
  `unavailable`. `freqctl.py` debe usar esta estrategia y nunca asumir
  `scaling_setspeed`.
- `frequency_control_paths` contiene los atributos reales por CPU/policy que
  E09 y `freqctl.py` pueden comprobar. No hardcodear rutas en nuevos módulos.
- `rapl_domains_available` usa alias únicos: `package-0`, `core-package-0`,
  `package-1`, `core-package-1`. `rapl_domain_paths` conserva la ruta sysfs.
  Los manifests y artefactos futuros deben usar esos alias, no `dram` ni `core`
  ambiguo.
- MAN-10 continúa exigiendo exactamente un nivel `native_governor`, denominado
  actualmente `REF`; los niveles `fixed` son los únicos que activan E07/E09.
- Una campaña nativa puede validar mecánica y calibración, pero si no hay
  control de frecuencia debe registrar `not_eligible_for_training_dataset=true`.
- El manifest de auditoría SC3 solo es una sonda. Un manifest real se materializa
  dentro de la asignación Slurm con CPUs y cgroup de workload reales.

### Decisiones que no deben presentarse como resueltas

- `smexa` y `exadell` no son destinos operativos mientras el requisito de GPU
  NVIDIA siga vigente, independientemente de sus capacidades CPU/RAPL.
- Un cgroup de Slurm que contiene al orquestador no satisface E03. Se necesita
  un hijo vacío para el workload; no relajar E03 para ocultar esa diferencia.
- No adaptar GPU AMD MI210 a checks NVIDIA mediante nombres ficticios. La ruta
  GPU futura se valida exclusivamente contra la GPU NVIDIA que Slurm asigne en
  `felix`.
- Temperatura de core, presupuesto Slurm, bytes proyectados, PMCs y binarios no
  pueden aprobarse por omisión. Deben obtenerse de una fuente real o bloquear.

### Próxima revisión documental requerida

Cuando se implemente `freqctl.py` y la integración Slurm, actualizar de forma
coherente la Guía, Checklist y Plan de Tests con ARC-11 a ARC-23. Incluir tests
de sysfs mockeado para cada estrategia y un protocolo de validación en hardware
que cubra permisos, restauración tras SIGINT y cgroup hijo. No cambiar la
semántica de F0/REF sin resolver ARC-15 en los cuatro documentos y en código.

## Cambios incorporados al diseño

| ID | Cambio o decisión | Motivo | Módulos / artefactos | Estado |
|---|---|---|---|---|
| ARC-01 | Configuración externa en `orchestrator.toml`. | Separar rutas sysfs, flags del harness y detección de tier del código y de secretos. | `config.py`, `environment.py`, `catalog.py`, `preflight.py`. | Implementado. |
| ARC-02 | Capa sysfs inyectable para pruebas. | Probar detección y preflight sin hardware ni rutas `/sys` reales. | `SysfsPaths`, argumentos `config` o rutas inyectables. | Implementado parcialmente; completar todos los checks nuevos. |
| ARC-03 | `environment.py` es la fuente única de capacidades. | Evitar detección duplicada y decisiones contradictorias entre módulos. | `environment.py`, metadata y preflight. | Implementado; se refuerza en ARC-12. |
| ARC-04 | Manifest exige `smt_policy` explícita. | El plan exige registrar la política SMT en metadata, no solo en logs. | `manifest.py`, `environment.py`. | Implementado. |
| ARC-05 | Diagnóstico de arranque HPC. | Verificar carga de manifest/catálogo y recopilar contexto real antes de ejecutar kernels. | `diagnostics.py`, `environment-hpc.yml`, plantilla SC3, `HPC_STARTUP.md`. | Implementado. |
| ARC-06 | El diagnóstico usa los CPUs efectivos del proceso mediante `--use-allowed-cpus`. | Los CPUs declarados en un ejemplo no representan necesariamente el cpuset concedido por Slurm. | `diagnostics.py`. | Implementado. |
| ARC-07 | Entorno Conda reproducible con Python 3.11. | SC3 ofrece Python 3.9 nativo; el orquestador requiere Python 3.11+. | `environment-hpc.yml`. | Implementado y comprobado en SC3. |
| ARC-08 | Manifest de auditoría separado de manifest de campaña. | La auditoría no ejecuta kernels ni posee cgroup de carga real. | `campaign_sc3_audit.yaml`. | Implementado. |
| ARC-09 | Preflight reducido acepta rutas/lectores inyectables. | Mantener pruebas unitarias sin tocar host y evitar rutas mágicas. | `preflight.py`, tests. | Implementado. |
| ARC-10 | Se distingue auditoría de campaña. | Que manifest y catálogo carguen no prueba C01/C02 ni habilita la ejecución. | Flujo de despliegue y documentación. | Implementado como política. |

## Hechos observados en SC3

### Perfil operativo común del clúster SC3

Las pruebas finales se ejecutarán mediante **Slurm 24.11.5** en nodos Rocky
Linux 9.7, kernel `5.14.0-611.36.1.el9_7.x86_64`, arquitectura `x86_64` y
cgroup v2 gestionado por `proctrack/cgroup`. Esta información procede de jobs
interactivos reales, no de una máquina local.

- El Python del sistema es 3.9; el orquestador usa Conda `hyperion-hpc`, con
  Python 3.11.15 y PyYAML.
- El cpuset efectivo de `os.sched_getaffinity(0)` es la única fuente autorizada
  para los CPUs de campaña. No sustituirlo por la topología global ni por
  `SLURM_CPUS_ON_NODE`.
- La ruta de cgroup cambia por job y step, por ejemplo
  `/sys/fs/cgroup/system.slice/<nodo>_slurmstepd.scope/job_<id>/step_<id>/user/task_0`.
  Una campaña real requiere un cgroup hijo de workload; el manifest de auditoría
  no es ejecutable como campaña.
- `perf` expone eventos genéricos de CPU. RAPL usa la jerarquía de kernel
  `intel-rapl` aunque los nodos sean AMD; el nombre de la ruta no identifica al
  fabricante.
- Hay GPUs AMD MI210 visibles. Visibilidad DRM no implica asignación exclusiva;
  los nodos `smexa` y `exadell` quedan descartados como destino final porque el
  objetivo requiere GPUs NVIDIA. GPU queda deshabilitada para estas pruebas de
  CPU; no invertir trabajo en ROCm para el pipeline final sin una decisión nueva.

### Aclaración normativa: cgroups de Slurm, orquestador y workload

El campo histórico `cgroup_path` no puede interpretarse como una ruta estática
del manifest. En SC3, Slurm crea una jerarquía dinámica por job y step. El
orquestador vive en el cgroup de su step, por lo que ese cgroup contiene al
menos el propio proceso y **no satisface E03** si E03 significa "cgroup de
workload vacío antes de iniciar la corrida".

La jerarquía requerida para una campaña real es:

```text
cgroup de la step Slurm (padre, contiene al orquestador)
└── cgroup hijo de campaña/workload (delegado al orquestador)
    └── cgroup hijo por run_id, opcional pero recomendado
        └── proceso del kernel y sus descendientes
```

Contratos actualizados:

- **E03 (campaña):** antes de la primera corrida, el cgroup hijo de workload
  debe existir, pertenecer a la asignación y tener `cgroup.procs` vacío. No se
  consulta `cgroup.procs` del cgroup padre de la step.
- **E06 (por corrida):** antes de mover/lanzar el kernel, el cgroup del
  `run_id` debe estar vacío; después de la corrida y antes de la siguiente,
  debe volver a estar vacío. Los únicos PIDs permitidos durante la corrida son
  el proceso del kernel y sus descendientes trazables.
- **Manifest:** declara una política o nombre lógico de cgroup, no una ruta
  absoluta de job. La ruta efectiva se resuelve después de entrar a la step
  Slurm y se guarda en metadata/reportes para reproducibilidad.
- **Seguridad:** el orquestador no debe crear ni administrar cgroups fuera de
  su subárbol delegado. Nunca debe mover PIDs ajenos ni modificar el cgroup
  padre de Slurm.

La implementación depende de que SC3 delegue controladores cgroup v2 al usuario
o suministre un helper Slurm restringido. La petición al administrador debe
incluir: crear un subárbol hijo por job, delegar `cgroup.procs`, `cpuset` y los
controladores estrictamente necesarios, y limitar cualquier helper a los CPUs y
PIDs de la asignación actual. Sin esa delegación, el pipeline puede observar el
cgroup de la step, pero no cumplir E03/E06 de aislamiento de workload.

### Resultados del diagnóstico actualizado

La rama `hpc-startup-diagnostic` añadió descubrimiento RAPL recursivo, consulta
de `policyN`, rutas cpufreq y separación entre soporte y permiso efectivo.

| Nodo | Afinidad observada | Frecuencia | RAPL | Decisión actual |
|---|---|---|---|---|
| `smexa.sc3.uis.edu.co` | AMD EPYC 9534; CPUs `0-3`, NUMA 0, siblings `128-131` fuera de la job. | `acpi-cpufreq`; 1500/1900/2450 MHz; rutas governor/min/max; escritura denegada. | Package y core para ambos sockets. | **Descartado:** GPU AMD, no NVIDIA. Evidencia histórica. |
| `exadell.sc3.uis.edu.co` | CPUs `30-33`, NUMA 0, siblings `158-161` fuera de la job. | No expone `cpuN/cpufreq` ni `policyN`; estrategia no disponible. | Package y core para ambos sockets. | **Descartado:** GPU AMD, no NVIDIA. Evidencia histórica. |
| `felix.sc3.uis.edu.co` | CPUs `0-3`, NUMA 0, siblings `32-35` fuera de la job; nodo con 4 NUMA. | `acpi-cpufreq`; diez niveles 1064–2261 MHz; rutas visibles; escritura denegada. | No expone RAPL. | **Único destino operativo actual.** Requiere confirmar y solicitar GPU NVIDIA por Slurm. |

Manifest y catálogo cargan en ambos nodos, pero esto **no** verifica C01/C02:
los binarios reales deben desplegarse y sus checksums actualizarse antes de una
campaña.

### Estado de problemas de la versión anterior

| Problema | Resolución | Evidencia |
|---|---|---|
| RAPL solo detectaba primer nivel. | Búsqueda recursiva y alias únicos. | `core-package-0` y `core-package-1` en ambos nodos. |
| No distinguía soporte cpufreq de permiso. | Nuevos campos de soporte, permiso, estrategia y rutas. | `smexa` soporta pero no puede escribir; `exadell` no expone interfaz. |
| Solo se buscaba `cpuN/cpufreq`. | También se consulta `cpufreq/policyN`. | El resultado de `exadell` ya no es falso negativo por esa ruta. |
| Campaña nativa exigía userspace/permisos. | E07/E09 solo aplican a niveles `fixed`. | Auditoría `native_governor` es solo lectura. |
| Faltaba contexto Slurm/cgroup/cpuset. | El diagnóstico lo serializa. | `startup_diagnostic.json` incluye job, cgroup y CPUs efectivos. |

Las limitaciones activas requieren autorización cpufreq en `felix`, un cgroup
hijo, una fuente de energía alternativa a RAPL, GPU NVIDIA asignada por Slurm y
binarios reales; no son defectos de detección.

### Nodo `smexa`

**Estado:** descartado por no disponer de GPU NVIDIA para las pruebas actuales.

- AMD EPYC 9534, 2 sockets / 2 NUMA, 128 cores físicos y SMT x2.
- `acpi-cpufreq`, frecuencias discretas visibles: 1500, 1900 y 2450 MHz.
- La asignación de prueba concedió CPUs efectivos `0-3`; todos pertenecen a
  NUMA 0 y sus siblings SMT `128-131` quedaron fuera de la asignación.
- RAPL cambia entre lecturas y es utilizable para paquetes; el informe externo
  también mostró subdominios core.
- Perf expone los ocho eventos genéricos de CPU y el usuario puede usar perf de
  espacio de usuario.
- GPU AMD MI210 visible, pero su visibilidad no demuestra asignación exclusiva
  por Slurm.
- El usuario de campaña no tenía confirmados permisos de escritura cpufreq.
- Cgroup observado en cada job: ruta dinámica bajo
  `/sys/fs/cgroup/system.slice/smexa_slurmstepd.scope/job_<id>/step_<id>/...`.

### Nodo `exadell`

**Estado:** descartado por no disponer de GPU NVIDIA para las pruebas actuales.

- El diagnóstico no detectó una interfaz cpufreq por CPU ni por `policyN` para
  la job. En el diseño actual, no existe una interfaz estándar que el
  orquestador pueda controlar desde esa asignación.
- No se debe extrapolar la capacidad de frecuencia de `smexa` a `exadell`.

### Nodo `felix`

**Estado:** único nodo operativo actual. Todo manifest y preflight debe partir
de su perfil, no de las capacidades históricas de `smexa` o `exadell`.

- Cuatro nodos NUMA; la asignación observada `0-3` está correctamente contenida
  en NUMA 0 y no incluye los siblings SMT `32-35`.
- Expone `acpi-cpufreq` con diez niveles discretos entre 1064 y 2261 MHz y las
  rutas de governor/límites, pero el usuario no puede escribirlas.
- `rapl_capable=false` y no existen dominios de energía. Por tanto, aun con una
  futura delegación cpufreq, el pipeline actual no puede calcular energía ni EDP
  en este nodo. No inventar ni imputar energía.
- Perf expone eventos genéricos más `mem-loads`; sirve para validar telemetría,
  afinidad, NUMA, Roofline y el flujo de artefactos en modo nativo.

## Enmiendas pendientes al plan y checklist

Las siguientes decisiones fueron motivadas por los resultados observados y se
deben implementar antes de declarar listo el preflight para campañas reales.

| ID | Enmienda | Regla afectada | Estado requerido |
|---|---|---|---|
| ARC-11 | Descubrir RAPL recursivamente y conservar un identificador único por dominio, junto con su ruta. | ENV-03, ENV-08, I05, I08. | Implementado; falta incorporar `max_energy_range_uj` por dominio en metadata. |
| ARC-12 | Separar capacidad de niveles, permiso efectivo y estrategia de frecuencia: `frequency_levels_supported`, `frequency_write_capable` y `frequency_control_strategy`. `freq_control_capable` no debe ocultar esa distinción. | ENV-02, ENV-05, E09, FRQ-06. | Implementado en `EnvironmentProfile`; falta que `freqctl.py` consuma la estrategia. |
| ARC-13 | Soportar políticas cpufreq: interfaz por CPU y por `policyN`; `acpi-cpufreq` debe usar los atributos que realmente controle, no asumir `scaling_setspeed`. | E07, E09, FRQ-02, FRQ-03. | Implementado para descubrimiento y E09; pendiente de aplicación en `freqctl.py`. |
| ARC-14 | Aplicar E07/E09 solo cuando el manifest solicite algún nivel `fixed`. Una campaña solo `native_governor` (F0 nativo) es de lectura y no exige `userspace`. | E07, E09, FRQ-06. | Implementado. |
| ARC-15 | Normalizar la nomenclatura: `REF` es el único nivel nativo según MAN-10; `F0...Fn` son niveles fijos. Si se adopta F0 nativo, se actualizarán conjuntamente MAN-10, ejemplos, metadata y tests. | MAN-10, FRQ-07, CAM-*. | Decisión pendiente antes de campañas. |
| ARC-16 | Validar el cpuset efectivo de Slurm contra todos los CPUs de campaña (delegados, collector y consumer). | E04, E05, nueva validación Slurm. | Pendiente. |
| ARC-17 | Usar un cgroup hijo de workload, vacío antes de cada corrida; no usar el cgroup de la step que contiene al orquestador. Resolver su ruta en tiempo de job. | MAN-01, E03, E06. | Pendiente; contrato de jerarquía definido y requiere acuerdo con administración SC3. |
| ARC-18 | Hacer E01 agnóstico de fabricante: Intel Turbo/HWP y AMD CPB/CPPC. Si no hay interfaz legible/controlable, registrar el estado y degradar la campaña a nativa en vez de afirmar control fijo. | E01, FRQ-01, FRQ-07. | Implementado para lectura/snapshot; pendiente la política de degradación en `freqctl.py`. |
| ARC-19 | Temperatura y procesos ajenos deben ser observados, no campos declarativos del manifest. Temperatura no disponible es advertencia; procesos ajenos se inspeccionan en cgroup/afinidad reales. | E02, E06. | Pendiente. |
| ARC-20 | No aprobar D05, I09 ni OPS-01 por datos ausentes. La capacidad PMC, proyección de bytes y presupuesto deben estar presentes o marcar el check como bloqueante/no verificable según tier. | D05, I09, OPS-01. | Implementado. D05 cerrado el 2026-08-01 con `environment.probe_pmc_count()` (detección empírica real, ver ARC-37); I09/OPS-01 dependen de que el manifest real declare `projected_campaign_bytes`/`remaining_core_hours`/`projected_core_hours` (siguen siendo entradas del planificador/operador, no auto-detectables como pmc_count). |
| ARC-21 | Normalizar E08 por CPUs efectivos/asignados y, cuando sea posible, medir el cgroup de campaña. | E08. | Implementado para CPUs delegados; pendiente medición específica del cgroup. |
| ARC-22 | Verificar GPU NVIDIA asignada por Slurm e implementar un inspector NVIDIA compatible. No usar ROCm ni los nodos AMD descartados mientras el requisito NVIDIA siga vigente. | G01, G02, G03. | Pendiente. |
| ARC-23 | C01/C02 se consideran verificados solo después de compilar/desplegar binarios en el nodo y actualizar checksums en catálogo. | CAT-01, CAT-02, C01, C02. | Pendiente operativo. |
| ARC-24 (2026-07-30) | F1.4 pide un mini INT-T11 local comparando `instructions` del launcher contra `perf stat -e instructions`; la máquina de desarrollo local no tiene el CLI `perf` instalado. Sustituto usado: comparar el total de `instructions` que reporta el launcher en modo `--exec` sobre un binario determinista (busy-loop de conteo fijo) contra una apertura independiente de `PerfReader` sobre el propio proceso (pid=0, mismo mecanismo PID+inherit) que ejecuta el mismo cómputo. Diferencia observada ≈0.05% (<5%, gate F1.4 cumplido). El INT-T11 real contra `perf stat` del sistema queda para F4.3 en felix, donde `perf 5.14` está confirmado disponible (Parte 0 del plan). | CPP-08, F1.4, F4.3 (INT-T11 real). | Implementado como sustituto local; INT-T11 real pendiente en felix. |
| ARC-25 (2026-07-30) | `runner.py` (F2.1) necesita saber dónde está el binario `telemetry_kernel_launcher` para construir el comando (RUN-01). `config.py` (ya "listo" según el plan) no tenía ese dato: se añadió `HarnessConfig.binary_path`, resuelto en `load_config()` relativo al propio `orchestrator.toml` si no es absoluto (mismo patrón que `manifest.load()` usa para `catalog_path`). Se agregó `harness.binary_path = "telemetry/build/telemetry_kernel_launcher"` a `orchestrator.toml`. Se actualizó el fixture TOML de `test_environment.py` para incluir la clave nueva (campo obligatorio). También se añadió `psutil>=5.9` a `environment-hpc.yml`, usado únicamente por `tests/orchestrator/test_runner.py` para verificar RUN-04 (ningún proceso hijo vivo tras timeout); `runner.py` en sí no depende de `psutil` en producción. | RUN-01, F2.1, config.py. | Implementado. |
| ARC-26 (2026-07-30) | F2.3 (`calibration.py`) requirió varias decisiones de diseño no explícitas en la Guía Maestra ni en `docs/orchestator/plan_v3/guia-tecnica.md`: (1) `catalog.py.KernelEntry` no tenía dónde declarar el regex de extracción de BW/FLOPs pedido por CAL-02/CAL-03 ("regex configurables en el catálogo"); se agregaron `bandwidth_stdout_pattern`/`flops_stdout_pattern`, validados en `__post_init__` (CAT-05 extendido) cuando el flag `reports_*_stdout` correspondiente es `True`. (2) El chequeo D03/CAL-04 necesita una "ficha técnica declarada en el manifest" que `manifest.py` tampoco tenía: se agregó `Manifest.hardware_datasheet` (opcional, `bw_pico_bytes_per_s`/`p_pico_flops_per_s`); si está ausente, D03 se trata como **fallo**, nunca como aprobado por falta de datos (consistente con ARC-20). (3) `run_calibration()` no hace conversión de unidades: el número que captura el regex se usa tal cual como `bw_pico_bytes_per_s`/`p_pico_flops_per_s`; la responsabilidad de que el regex capture el valor ya en esas unidades (o de documentar la conversión) queda en quien escribe la entrada del catálogo. (4) `CalibrationReferences` (guia-tecnica.md) define un único `cv_pct`/`accepted` para cuatro métricas (IPC/IPS/MPKI/MissRate); se implementó como el **máximo** de los cuatro coeficientes de variación individuales (criterio conservador: cualquier métrica inestable descalifica la referencia) por no haber una definición más específica en ninguna de las guías. `build_node_profile()` también fusiona el `NodeProfile` de la guía técnica (topología/cache) con el `pmc_count` que ya consumía `preflight.py` (D05), en vez de crear un dataclass paralelo. | CAL-01..11, CAT-05, MAN-00. | Implementado; decisiones documentadas para revisión humana. |
| ARC-27 (2026-07-30, confirmado 2026-07-30) | F2.5 (`postprocess.py`) requirió varias decisiones no explícitas en ninguna guía: (1) **Divergencia resuelta, confirmada por el director:** `docs/orchestator/plan_v3/guia-tecnica.md` (línea ~710) prorratea `flops_window_estimate` por duración (`run_flops_total * delta_t_ns/run_duration_ns`); `docs/retoma/Plan_Implementacion_Medicion_SC3.md` (F2.5, el documento ejecutable subordinado a la Guía Maestra) pide prorratear por `delta_instructions`. La Guía Maestra autoritativa (`Guia_Maestra_Fase1_DVFS.md`) no especifica el método, solo que el origen debe ser el stdout del binario (POST-09). Se implementó y se confirmó el prorrateo por `delta_instructions`: los FLOPs son un subconjunto de las instrucciones retiradas, mientras que el tiempo de reloj está confundido por los stalls de memoria — exactamente el efecto que `phase_label_train` necesita discriminar entre fases compute/memory-bound. Prorratear por tiempo asignaría FLOPs proporcionalmente altos a ventanas memory-bound (que duran mucho pero retiran pocas instrucciones), sesgando la intensidad operacional en la dirección equivocada. `guia-tecnica.md` queda documentado como método descartado, no como pendiente. (2) Los 7 valores de `quality_status` no alcanzan para cubrir todas las condiciones de POST-01..10 simultáneamente en una sola columna; se definió una prioridad propia (`first_sample_no_delta` > `pmu_degraded` > `warmup_excluded` > `intensity_undefined` > `energy_invalid` > `no_freq_reading` > `ok`), documentada en el código. (3) `quality_status='energy_invalid'` solo se activa si el manifest declaró `rapl_enabled=True` para esa campaña; en felix (sin RAPL para siempre) nunca se activa por defecto, evitando que todas las ventanas queden marcadas como inválidas solo por la ausencia estructural de RAPL — `energy_valid` (columna aparte) sigue siendo `False` en cada fila igualmente. (4) Nuevos campos requeridos: `catalog.KernelEntry.flops_total_stdout_pattern` (opcional, regex para el total de FLOPs de kernels dataset) y `NodeProfile.cache_line_size_bytes` (leído de `coherency_line_size`, POST-10). (5) El emparejamiento de filas ENERGY con la ventana CPU correspondiente se hace por barrido de dos punteros sobre timestamp (ambas listas ordenadas), no por índice posicional, para tolerar lecturas RAPL fallidas ocasionales sin desalinear el resto de la corrida. | POST-01..16, CAL-02/03. | Implementado; decisión de ARC-27(1) confirmada por el director. |
| ARC-28 (2026-07-31) | Hallazgo al revisar si el pipeline recorre los niveles de frecuencia propuestos (F0/F1/F2/REF): `campaign.build_matrix()` sí itera sobre todos los `frequency_levels` del manifest y `freqctl.apply_frequency()` sí implementa y verifica ambas estrategias (discrete_bounds/bounded_range), pero el resultado (`AppliedFrequency.requested_khz`/`applied_khz`, exigido literalmente por FRQ-03) se calculaba y se descartaba: `runner.py` invocaba `apply_frequency(...)` sin guardar el retorno, y `campaign.py` no pasaba `freq_khz_requested`/`freq_khz_applied`/`freq_khz_observed` a `postprocess.run_postprocess()`. En felix hoy es inofensivo (`frequency_write_capable=False`, `apply_frequency` nunca se invoca), pero habría producido `windows.csv` con esas columnas vacías en cuanto H1 conceda escritura de cpufreq. Corregido: `RunResult` gana el campo `applied_frequency`; `runner._merge_metadata()` lo funde en la metadata.json de la corrida; `campaign.py` lo propaga a `run_postprocess()` junto con una lectura de `freqctl.read_observed_frequency_khz()` tomada una vez por corrida (no por ventana individual — el harness C++ no muestrea `scaling_cur_freq` por tick de perf, ver nota FRQ-10). | FRQ-03, FRQ-10, RUN-06, MET-07. | Corregido. |
| ARC-29 (2026-07-31) | Auditoría adicional de solo lectura en felix (`ssh guane` + `srun`), motivada por la pregunta de si el control de frecuencia podría afectar reservas de otros usuarios. Hallazgo crítico: `freqdomain_cpus` de cpu0 = `0-7,32-39` — el dominio de frecuencia de `acpi-cpufreq` en felix es **por socket completo** (8 cores físicos + sus 8 SMT), no por core individual. Una asignación Slurm que no cubra el socket completo (ej. la de 4 CPUs auditada el 2026-07-30, cpuset `0-1,32-33`, subconjunto estricto del dominio) compartiría el dominio de frecuencia con cualquier otro job en el resto del socket — fijar `scaling_setspeed` en esos cores cambiaría la frecuencia de cores ajenos también. No hay ningún check de preflight (E01-E09 actuales) que verifique esto; todos verifican contaminación de *procesos*, no de *dominio de frecuencia compartido*. Se propone (no implementado) un nuevo check de preflight que compare `delegated_cpus` contra el `freqdomain_cpus`/`related_cpus` real leído de sysfs, bloqueante si `delegated_cpus` es un subconjunto estricto del dominio en cualquier CPU delegado. Se actualizó H1 para pedir explícitamente que la asignación cubra el dominio completo. Hallazgos adicionales de la misma auditoría: `userspace` SÍ está en `scaling_available_governors` (H1 es netamente un tema de permisos); `/scratch` NO es escribible para el usuario (contradice la suposición de F4.1); `~/hyperion` en el clúster está anidado en `~/hyperion/hyperion/` (artefacto de rsync) y desactualizado (último commit sincronizado 2026-07-13, no incluye Fase 1/Fase 2 de esta sesión); GPU confirmada (`--gres=gpu:1`) resolviendo H5; topología de cache/cpuinfo real coincide exactamente con lo que `node_profile.py` ya calculaba contra mocks. Todo esto es específico de hardware Nehalem-EX (2010); un clúster más moderno con `intel_pstate`/HWP probablemente tiene dominios por-core, pero el check propuesto debe seguir siendo genérico (leer el dominio real de sysfs, no asumir "todo el socket"). | Preflight (E01-E09, check nuevo propuesto), H1, H5, F4.1. | Hallazgo documentado; check de preflight nuevo pendiente de implementar. |
| ARC-30 (2026-07-31) | Implementado el check de preflight propuesto en ARC-29. `environment.py` gana `_frequency_domain_data()`, que lee `freqdomain_cpus` (o, si no existe, `related_cpus`/`affected_cpus`, en ese orden — cubre tanto el hardware antiguo de felix como drivers modernos que puedan exponer solo uno de los tres) por cada CPU delegado y lo expone como `EnvironmentProfile.frequency_domain_cpus: dict[int, list[int]]` (atributo dinámico, mismo patrón que `numa_cpu_map`/`delegated_cpu_numa_nodes`); se incluye en `environment_report.json`. `preflight.py` gana `check_frequency_domain()` (factor_id **E10**, nueva regla — no exigida por ninguna guía anterior), bloqueante, que falla si el dominio real de algún CPU delegado excede el conjunto de `delegated_cpus`; se ejecuta en `run_campaign_preflight()` únicamente cuando la campaña solicita algún nivel `fixed` (mismo gate que E07/E09). Decisión de diseño: si no hay datos de dominio disponibles (archivo ausente, ej. drivers modernos sin esa exposición), el check pasa sin bloquear — la ausencia de evidencia no debe impedir campañas en clústeres donde el problema no aplica; solo bloquea cuando el dominio leído demuestra explícitamente una fuga. Cubierto por `test_env_t11/t12/t13` (`test_environment.py`) y `test_e10_*` (`test_preflight.py`), 3 tests nuevos cada uno. Checklist actualizado: PRE-E10 (nuevo) marcado ☑ en §12.4 de la Guía Maestra, contador de la sección actualizado de 25 a 26 reglas. | E10 (nuevo), environment.py, preflight.py, Guía Maestra §12.4. | Implementado. |
| ARC-31 (2026-07-31) | F3.1 pedía compilar ERT en felix, con la alternativa explícita (CAL-03) de "un microbenchmark de FLOPs pico de suite reconocida" si el despliegue resultaba pesado, a condición de registrar la decisión como enmienda ARC. Se inspeccionó el repositorio oficial de ERT (Berkeley Lab CS Roofline Toolkit, `bitbucket.org/berkeleylab/cs-roofline-toolkit`): el driver completo (v1.1.0) requiere plantillas `Batch/`+`Config/` especificas por máquina (existen para NERSC/ANL/ORNL, ninguna para SC3/felix) y un driver Python (`roofline.py`) que orquesta un barrido de muchos jobs y post-procesa los resultados fuera de línea — desplegarlo correctamente en un Slurm que no administramos, sin iteración interactiva rápida (cada intento de config cuesta un round-trip de `srun`), se consideró desproporcionado para obtener un solo número de FLOPs pico por calibración. Se tomó la alternativa aprobada: `kernels/ert/ert_probe.c`, un driver propio y autocontenido que reimplementa la aritmética real de ERT (`Kernels/kernel1.c` v1.0.0, `ERT_FLOP=16`: `beta = beta*A[i] + alpha` desenrollado 8 veces) sobre un buffer que cabe en cache, barriendo tamaño de working set y repeticiones, e imprimiendo directamente `GFLOPs/sec: <pico>` por stdout — sin MPI/BGQ/QPX/intrinsics AVX ni el post-proceso Python. Compilado y verificado en felix (8 cores, `--hint=nomultithread`): `GFLOPs/sec: 31.939` con working set de 25294 doubles/hilo. | CAL-03, F3.1, ert\_probe. | Implementado. |
| ARC-32 (2026-07-31) | F3.1-F3.3 ejecutados de punta a punta en felix (vía `srun --partition=gpu_titan --nodelist=felix --cpus-per-task=8 --hint=nomultithread`, cubriendo el socket completo por E10/ARC-30). (1) **NPB**: descargado `NPB3.4.4.tar.gz` de nas.nasa.gov (sha256 registrado en `catalog.yaml`), compilado con el módulo `gnu14` y `config/make.def.template` sin modificar (`FFLAGS`/`CFLAGS` = `-O3 -fopenmp` ya son el default del suite). (2) **Selección de clase**: clase S (todas <0.1s) y clase A (todas <1.5s salvo LU en 7.1s) quedan muy por debajo del objetivo aspiracional de ≥60s del plan F3.1; clase B se midió real: ep=5.9s, mg=2.6s, cg=21.7-22.8s, is=0.8-1.6s, ft=11.1-11.9s, lu=35.5-35.9s. Ninguna clase estándar llega a 60s de forma uniforme en las 6, y clase C llevaría el total de la matriz de campaña muy por encima del presupuesto razonable sin necesidad real: con muestreo a 1ms (`--interval-ns 1000000`, confirmado en Guía Maestra §10.4) el requisito duro es ≥50 ventanas útiles tras warmup, y clase B ya da entre ~700 (is) y ~35000 (lu) ventanas por corrida, 10-700× el mínimo. Se eligió **clase B para las 6 (dataset)**, dejando clase C como opción futura si el director (H4) pide series más densas. (3) **STREAM**: `STREAM_ARRAY_SIZE=64000000` (~512 MB/arreglo, muy por encima de los 24 MB de L3 por socket de felix, con holgura amplia para no depender de si la campaña termina siendo mono- o multi-socket). (4) **Regex de éxito NPB**: las 6 suites comparten la misma línea final de resumen `Verification    =               SUCCESSFUL` (de la rutina común `print_results`) — se usó ese patrón uniforme en vez del sugerido originalmente en el plan ("VERIFICATION SUCCESSFUL", que solo aparece en el cuerpo de MG/CG, con formato distinto en FT/LU/IS). (5) **POST-09/FLOPs**: confirmado que NPB nunca imprime un total absoluto de FLOPs, solo `Mop/s total` (tasa) y `Time in seconds`; se agregaron los campos `flops_rate_stdout_pattern`/`runtime_seconds_stdout_pattern` a `KernelEntry` y el fallback correspondiente (tasa × 1e6 × tiempo) en `postprocess.extract_run_flops_total()`, activo cuando `flops_total_stdout_pattern` está ausente — cambio menor, ya anticipado por el propio texto del plan F3.2 ("Mop/s total × tiempo"). `orchestrator/schemas/kernels/catalog.yaml` quedó con los 8 checksums reales (2 calibration + 6 dataset) y las 6 clases CAT-01..08 pasaron a ☑. | CAT-01..08, POST-09, F3.1, F3.2, F3.3, catalog.py, postprocess.py. | Implementado. F3.4 (validación de bytes bajo el launcher C++) ejecutada por separado, ver ARC-33. |
| ARC-34 (2026-08-01) | El gate de F4.4 ("overhead baseline-vs-telemetry estable (CV < 10%)") no tenía ningún código que lo calculara: `campaign.py` ya programaba el par baseline+telemetry (CAM-04) y medía `elapsed_seconds` de ambos, pero el resultado del baseline se descartaba (`continue  # solo mide overhead; no se valida ni se postprocesa`) sin que nada leyera ese número de vuelta. Corregido: `CampaignProgress` gana `overhead_pct_values: list[float]`; dentro de `run_campaign()` se guarda `baseline_elapsed_seconds` al procesar el ítem `"baseline"` del par y, al procesar `"telemetry"`, se calcula `(telemetry.elapsed_seconds - baseline_elapsed_seconds) / baseline_elapsed_seconds * 100` y se acumula — solo para pares que realmente corrieron esta ejecución, nunca para combinaciones reanudadas por CAM-03 (ahí no hay baseline nuevo que comparar). `write_campaign_metadata()` persiste la lista en `campaign_metadata.json`. `report.py` gana `overhead_stats()` (media/CV/muestras, siguiendo el mismo patrón que `_cv_pct` de `calibration.py`) y `overhead_stability_warning()` (advertencia no bloqueante si CV > 10%, mismo patrón que `calibration_stability_warning`/MET-05); `build_report()` los incluye siempre. `cli.py::cmd_report()` lee `overhead_pct_values` de `campaign_metadata.json` y lo pasa a `build_report()`. 5 tests nuevos (`test_campaign.py`, `test_report.py`, `test_cli.py`). | CAM-08 (nuevo), campaign.py, report.py, cli.py. | Implementado. |
| ARC-35 (2026-08-01) | Auditoría de solo lectura en felix para evaluar si los contadores de uncore/memory-controller (propuestos en ARC-33 como validación cruzada del sesgo de bytes de F3.4) son accesibles hoy. Confirmado: el nodo expone 16 PMU de uncore en `/sys/bus/event_source/devices/` (`uncore_mbox_0/1` es el bloque de memoria en esta arquitectura Nehalem-EX/Westmere-EX — equivalente a "iMC" en generaciones más nuevas de Intel; también `uncore_cbox_0..7`, `uncore_bbox_0/1`, `uncore_rbox_0/1`, `uncore_sbox_0/1`, `uncore_ubox`, `uncore_wbox`), pero **el usuario no puede abrirlos**: `perf stat -e uncore_ubox/event=0x1/ -a -- sleep 1` falla con `perf_event_paranoid setting is 1: >=1: Disallow CPU event access` — los eventos de uncore son de alcance socket/sistema completo (nunca por PID como los que ya usamos), y ese nivel de `perf_event_paranoid` los bloquea para usuarios sin `CAP_PERFMON`/`CAP_SYS_PTRACE`/`CAP_SYS_ADMIN`. También confirmado: LIKWID no está instalado como módulo, y `/dev/cpu/*/msr` es `root:root` sin acceso de usuario (bloquea la ruta alternativa de LIKWID, que lee MSR directamente en vez de pasar por `perf_event_open`). Se agregó **P4** a `Solicitud_Permisos_SC3.md` pidiendo bajar `perf_event_paranoid` a 0/-1 o `CAP_PERFMON` en el binario `perf`, con la evidencia exacta de este hallazgo. | P4 (permisos), ARC-33 (validación cruzada pendiente). | Hallazgo documentado; permiso solicitado, no concedido todavía. |
| ARC-36 (2026-08-01) | F4.2: primer `environment.detect_environment()` y `preflight.run_campaign_preflight()` reales contra felix (nunca antes ejecutados fuera de mocks — ver Handoff, sección "Estado del proyecto"), con `delegated_cpus=0-5` y el entorno Conda `hyperion-hpc` ya existente en el clúster (`pyyaml`/`pytest` disponibles; `psutil` sigue ausente pero solo lo usa `test_runner.py`, no producción). Encontró y corrigió dos bloqueadores reales: (1) **bug de seguridad real**: `frequency_domain_cpus` salía vacío para cada CPU delegado porque `_parse_cpu_list()` solo entendía el formato `"0-7,32-39"` (rangos con coma) y `freqdomain_cpus`/`related_cpus` en felix usan una lista plana separada por espacios (`"0 1 2 ... 39"`) — el `ValueError` se descartaba en silencio y el check E10 (ARC-30, la protección contra fuga de control de frecuencia a otro usuario del mismo socket) nunca tenía datos con qué bloquear nada, aunque los 6 tests unitarios de E10 seguían en verde porque ninguno probó ese formato real. Corregido en `_parse_cpu_list()`; test de regresión con el string exacto de felix. (2) `preflight.check_memory_size` (C05) exige `estimated_memory_bytes` en TODO kernel del catálogo (calibración incluida), pero `catalog.py` (CAT-04) solo lo requiere para `role=dataset` — sin el campo, C05 bloqueaba cualquier preflight real de forma permanente en `stream_official`/`ert_probe`. Corregido agregando el campo a esas dos entradas en `catalog.yaml`, con valores derivados del tamaño real conocido de sus buffers (STREAM: 3×8B×STREAM_ARRAY_SIZE; ert_probe: buffer máximo del barrido × hilos, con margen). Con ambos fixes, el preflight completo (17 checks) queda en verde salvo **D05** (capacidad de PMC), que sigue bloqueado porque `pmc_count` nunca se implementó en `environment.py` (`node_profile.py` lee `getattr(env, "pmc_count", 0)`, que siempre da 0) — gap ya documentado en ARC-20, confirmado que sigue pendiente, no es un hallazgo nuevo. `environment_report.json`/`node_profile.json` reales quedaron escritos en `~/hyperion-kernels/f42_preflight_test/` en felix como evidencia. | E10, C05, D05, environment.py, catalog.yaml, F4.2. | Corregido (freqdomain_cpus, C05); D05 cerrado por separado el mismo día — ver ARC-37. Preflight completo (42 checks con el catálogo real de 8 kernels) verificado en verde el 2026-08-01. |
| ARC-37 (2026-08-01) | Cierre de D05 (pendiente desde ARC-20 y confirmado de nuevo en ARC-36): `environment.py` gana `probe_pmc_count()`, que mide empíricamente cuántos eventos genéricos de `PERF_TYPE_HARDWARE` (los 10 definidos por el kernel: instructions, cycles, cache-references, cache-misses, branch-instructions, branch-misses, bus-cycles, ref-cycles, stalled-cycles-frontend, stalled-cycles-backend) el nodo puede programar simultáneamente sin multiplexar. Verificado a mano en felix antes de implementar: `perf stat -e <N eventos> -- sleep 0.3` no muestra evidencia de multiplexado hasta N=5; en N=6 aparece `<not counted>` en `branch-misses` y anotaciones de porcentaje (`(XX.XX%)`, la forma en que `perf stat` expone `time_running < time_enabled`) en el resto — felix soporta exactamente **5** contadores simultáneos. `probe_pmc_count()` reproduce ese mismo criterio de forma genérica (nunca hardcodea "5" ni asume un modelo de CPU, consistente con la regla del Handoff "PMCs deben obtenerse de una fuente real o bloquear"): incrementa N y se detiene en el primer N que muestra esa evidencia; si `perf` no está disponible retorna 0 (D05 bloquea con datos ausentes, no aprueba por omisión). No se integró dentro de `detect_environment()` (que debe seguir siendo ENV-01, lectura pura de sysfs) sino en `cli.py::_detect_environment()`, que lo corre aparte después de la detección y antes de pasar el perfil a `preflight`/`campaign`. Verificado en felix: `probe_pmc_count()` devuelve 5 (coincide con la prueba manual), y con eso el preflight completo (42 checks, catálogo real de 8 kernels) queda en **verde total por primera vez**. | D05, environment.py, cli.py. | Implementado y verificado en hardware real. |
| ARC-38 (2026-08-01) | Séptimo kernel del dataset agregado al catálogo siguiendo la recomendación de `docs/retoma/Propuesta_Seleccion_Kernels_Dataset.md` (confirmada por el usuario: "vamos a coger las recomendaciones que haces en la propuesta"): DGEMM sobre OpenBLAS. `kernels/dgemm/dgemm_bench.c` es un driver propio sobre `cblas_dgemm` (no reimplementa álgebra lineal); confirmado `openblas-devel` instalado en felix sin necesidad de módulo (`pkg-config --cflags --libs openblas` funciona directo). El tamaño de matriz se pasa por CLI (`--size`, no fijo en compilación) para poder agregar más `size_variant` más adelante sin recompilar, tal como proponía la sección 4 de la propuesta. Verificación: en vez de recalcular la matriz completa (mismo costo que el cómputo), se compara una muestra aleatoria de 64 entradas de C contra el producto punto directo correspondiente (tolerancia relativa 1e-8). Salida en el mismo formato que los kernels NPB (`Time in seconds`/`Mop/s total`/`Verification`) para reusar exactamente los mismos `success_check`/`flops_rate_stdout_pattern`/`runtime_seconds_stdout_pattern` ya validados — sin tocar código de catálogo ni postprocesamiento. Compilado y verificado en felix: N=2048, 6 iteraciones, ~12.18 s, `Verification SUCCESSFUL`, ~8.46 GFLOP/s sostenido con 6 hilos (`OPENBLAS_NUM_THREADS=6`). Checksum real en `catalog.yaml`. Reposicionar en el plano Roofline exacto (qué tan cerca del ridge cae) queda pendiente de la calibración real (F4.2/F4.3, `i_ridge` no medido todavía) — la elección de N=2048 fue por runtime razonable, no por intensidad operacional objetivo. Preflight completo re-verificado con el catálogo de 7 kernels: 46/46 checks en verde. | CAT-01, F3.1, F3.2, Propuesta\_Seleccion\_Kernels\_Dataset.md. | Implementado y verificado en hardware real. |
| ARC-39 (2026-08-01) | Reorganización de `$HOME` en felix: los scripts operativos (`build_kernels*.sh`, `build_dgemm.sh`, `build_and_run_f34.sh`, `f42_preflight.py`, `felix_audit.sh`) se habían ido acumulando sueltos en la raíz de `$HOME` en cada sesión, sin versionar — invisibles para cualquiera que audite el repo, y sin garantía de reproducirse igual en otro clúster (contradice el objetivo explícito de portabilidad discutido con el usuario). Movidos y consolidados a `scripts/felix/` en el repo (versionados, documentados en `scripts/felix/README.md`): `build_npb.sh` (unifica los 3 scripts de build de clases NPB en uno parametrizado por clase), `build_stream_ert.sh`, `build_dgemm.sh`, `run_f34_validation.sh`, `run_preflight_check.py`, `audit_readonly.sh`. Se estableció además una convención explícita de directorios para `$HOME` mientras `/scratch` siga sin estar disponible (P3, sin responder todavía): `~/hyperion-kernels/{src,bin}` solo para binarios/fuente de kernels (sin cambios), `~/hyperion-results/{validation,campaigns}` nuevo — separando corridas de validación puntuales (F3.4, F4.2, antes mezcladas dentro de `hyperion-kernels/`) del futuro `output_dir` real de campañas. Los directorios de validación existentes (`f34_stream_1785479291/`, `f42_preflight_test/`) se movieron a `~/hyperion-results/validation/` conservando su nombre para no romper la trazabilidad con ARC-33/ARC-36. Scripts sueltos viejos en `$HOME` eliminados tras confirmar que la copia versionada en `~/hyperion/scripts/felix/` (vía `git pull`) los reemplaza. | scripts/felix/, convención de `$HOME` en felix. | Implementado. |
| ARC-40 (2026-08-01) | Hallazgo del usuario antes de correr el piloto: PID+inherit resuelve **atribución** correcta de las muestras (confirmado en F3.4 — el harness coincide con `perf stat` nativo dentro de 3.5%), pero eso no protege contra **contención física** de caché L3/ancho de banda de memoria con otro proceso corriendo en los mismos cores — un efecto distinto que solo se detecta verificando `Cpus_allowed` real de procesos vivos, nunca por membresía de cgroup (PRE-E06). Al revisar el código antes de confiar en el gate se encontraron dos huecos reales, no solo checklist desactualizado (la auditoría del 2026-08-01 había marcado PRE-E06 ☑ solo porque `check_foreign_processes()` tenía test — pero ese test le pasaba la lista de PIDs ya armada, nunca la calculaba): (1) no existía ningún código que escaneara `/proc/*/status` en busca de `Cpus_allowed` real; (2) `run_reduced_preflight()` (que agrupa E06 con E01/E02/E07/E08/I07/C01/C02) **nunca se invocaba desde `campaign.py`** — todo el "preflight reducido por corrida" era código muerto, nunca ejecutado en la ruta real de una campaña. Corregido: `preflight.detect_foreign_affinity_pids(delegated_cpus, proc_root=..., own_pids=...)` escanea `/proc/<pid>/status` (línea `Cpus_allowed:`, formato hex con comas para más de 32 bits) y `/proc/<pid>/cmdline` (vacío → hilo de kernel, se ignora); `campaign.py` lo corre justo antes de programar cada par baseline+telemetry (no dentro de `run_reduced_preflight()` completo, que sigue sin invocarse — ver limitación abajo) y, si hay solape, rechaza la combinación completa (factor\_id E06, sin ejecutar ni baseline ni telemetry) sin gastar tiempo de cómputo en una medición que ya se sabe contaminada. `own_pids` excluye el PID del propio orquestador (que normalmente hereda el cpuset completo del job y se auto-marcaría como "ajeno" si no se excluye). 6 tests nuevos con un `/proc` falso (incluye el formato real con comas de sistemas de muchos cores) + 2 tests de integración en `campaign.py`. **Limitación que queda pendiente:** el resto de `run_reduced_preflight()` (E01 deriva de Turbo/HWP, E02 temperatura, E07 governor, E08 carga externa, I07 run\_id único, C01/C02 binario/checksum por corrida) sigue sin invocarse desde `campaign.py` — solo se extrajo y conectó el chequeo E06 que pidió el usuario explícitamente. Conectar el resto queda como trabajo futuro, no se asumió como parte de este pedido puntual. | E06, campaign.py, preflight.py. | E06 implementado y verificado (176→184 tests); el resto de run\_reduced\_preflight sigue sin conectar (limitación documentada, no resuelta). |
| ARC-41 (2026-08-01) | Al armar el manifest real para el piloto (F4.4), `environment_tier: hpc_sc3` exigía `cgroup_path` no nulo (MAN-01) para que el preflight de campaña pudiera validar E03 (cgroup HIJO de workload vacío). Se intentó crear un cgroup hijo bajo el cgroup del job en felix (`mkdir` dentro de `/sys/fs/cgroup/.../job_.../step_0/user/task_0/`) y dio `Permission denied` — el usuario no tiene delegación de cgroup en SC3, no hay forma de satisfacer ese requisito hoy. En vez de pedir un permiso nuevo o forzar un cgroup no-hijo (explícitamente prohibido por el Handoff: "Un cgroup de Slurm que contiene al orquestador no satisface E03... no relajar E03 para ocultar esa diferencia"), se revisó si el requisito seguía vigente: **no**. El texto de MAN-01 en la Guía Maestra siempre dijo "cgroup\_path es OPCIONAL en todos los tiers" — el código tenía una excepción no documentada ahí que lo exigía para hpc\_sc3, un resabio de cuando perf se adjuntaba por cgroup, antes de la migración a PID+inherit (Fase 1, CPP-01..08). Con E06 (ARC-40, `detect_foreign_affinity_pids` por `Cpus_allowed` real) ya implementado, el propósito original de la verificación de cgroup (detectar contaminación por procesos ajenos) queda cubierto por un mecanismo estrictamente más fuerte: no depende de membresía de cgroup (que ni siquiera garantiza afinidad real de CPU si el cpuset no está particionado estrictamente) ni de tener delegación de cgroup del clúster. Se eliminó la excepción en `manifest.py` — `cgroup_path` vuelve a ser opcional en todos los tiers, tal como ya decía la regla. `check_cgroup_clean`/E03 se sigue ejecutando igual si se declara un `cgroup_path` real (nunca se eliminó esa capacidad, solo dejó de ser obligatoria para hpc\_sc3). Decisión tomada explícitamente, no un parche silencioso — confirmada con el usuario antes de implementarla. | MAN-01, manifest.py. | Implementado; `test_man_t02_hpc_sc3_no_requiere_cgroup` cubre el caso nuevo. |
| ARC-42 (2026-08-01) | Primera corrida real de `campaign.run_campaign()` contra felix (F4.4): `run_calibration()` falló con "CAL-02: la calibración de ancho de banda (stream_official) no tuvo éxito" pese a que `stream_c` corrió, terminó con exit 0 y su propio `Solution Validates` confirmaba éxito. Causa raíz en `telemetry_kernel_launcher.cpp`: `run_child()` captura el stdout del proceso medido en `ChildResult::output`, pero ese valor **solo se usaba en las ramas de fallo** (`telemetry.exit_code != 0`, para el mensaje de error) — en el camino exitoso se descartaba en silencio, nunca se escribía a ningún lado. `runner.py` (Python) escribe `run_dir/stdout.txt` capturando el stdout del PROCESO DEL LAUNCHER (vía `subprocess`), que es distinto del stdout del hijo medido (el launcher lo redirige a un pipe interno con `dup2`); como el launcher nunca reenviaba ese contenido, `stdout.txt` solo tenía la línea de resumen propia del launcher (`run_dir=...`), nunca `Triad:`/`Verification`/etc. — rompiendo silenciosamente CAL-02/CAL-03/POST-09, que dependen enteramente de leer `stdout.txt` para extraer BW\_pico/P\_pico/FLOPs por regex. Ningún test unitario lo detectó porque `calibration.py`/`postprocess.py` siempre se probaron con fakes que ya proveían el stdout esperado directamente — ninguno ejercitó el binario C++ real compilado. Corregido: `std::fputs(telemetry.output.c_str(), stdout)` justo después de confirmar `exit_code==0`, en el modo `--exec` (el que usan todos los kernels reales) y en el modo `--kernel` sintético. Recompilado y re-verificado: los 10 CTest siguen pasando. | CAL-02, CAL-03, POST-09, telemetry\_kernel\_launcher.cpp. | Corregido y verificado en hardware real. |
| ARC-43 (2026-08-01) | Segundo hallazgo de la misma corrida, después de ARC-42: la calibración corrió y extrajo BW\_pico/P\_pico reales del stdout, pero D03 falló — `BW_pico observado=1.420e+04` vs `1.472e+10` declarado, `P_pico observado=3.075e+01` vs `2.397e+10` declarado, ambos fuera por un factor de exactamente ~1e6/~1e9. Causa: `calibration.py` guardaba el número que captura el regex TAL CUAL como `bw_pico_bytes_per_s`/`p_pico_flops_per_s`, sin convertir — pero STREAM imprime `Triad:` en **MB/s**, no B/s, y `ert_probe` imprime `GFLOPs/sec:`, no FLOP/s. Esto no era solo un problema de comparación contra `hardware_datasheet`: `i_ridge = p_pico/bw_pico` con GFLOP/s sobre MB/s da un resultado **1000× menor** que el flops/byte real (los prefijos GFLOP/s=1e9 y MB/s=1e6 no cancelan al dividir), así que el ridge point de CUALQUIER calibración futura habría quedado sistemáticamente corrupto sin que ningún check lo detectara — D03 solo lo atrapó esta vez porque el `hardware_datasheet` de referencia sí estaba en unidades SI correctas. Un test previo (`test_cal01_cal02_cal03...`) documentaba explícitamente en un comentario que "run\_calibration no hace conversión de unidades por su cuenta" — una decisión de diseño real pero incompleta, nunca ejercitada contra binarios que imprimen en unidades no-SI como STREAM/ert\_probe. Corregido: `KernelEntry` gana `bandwidth_stdout_unit_multiplier`/`flops_stdout_unit_multiplier` (default 1.0, sin cambio de comportamiento para catálogos existentes), `calibration.py` multiplica el valor extraído antes de guardarlo. `catalog.yaml`: `stream_official` con multiplicador `1000000` (MB/s→B/s), `ert_probe` con `1000000000` (GFLOP/s→FLOP/s). 3 tests nuevos (`test_catalog.py`, `test_calibration.py`) cubren el multiplicador default y el caso no-trivial con los números reales de STREAM/ert\_probe. | CAL-02, CAL-03, catalog.py, calibration.py. | Corregido; pendiente re-verificar D03 en verde con el piloto real (siguiente paso). |
| ARC-44 (2026-08-01) | Tercer hallazgo de la misma corrida real, después de ARC-42/ARC-43: con los dos bugs anteriores corregidos, la campaña corrió de punta a punta sin crashear pero rechazó **las 6 combinaciones** por E06 ("procesos ajenos con afinidad a delegated\_cpus"), 0 aceptadas. La lista de PIDs "ajenos" tenía decenas de entradas (`1, 1176, 1192, 1589, ...`) — revisando qué eran, resultaron ser daemons y procesos comunes del sistema en reposo, ninguno compitiendo activamente por nada. Causa: `detect_foreign_affinity_pids()` (ARC-40) filtraba por `Cpus_allowed`, la máscara de CPUs donde el scheduler *podría* poner un proceso — pero casi ningún proceso en un sistema Linux real restringe esa máscara (queda en "todos los CPUs" por defecto salvo que alguien la fije explícitamente), así que prácticamente cualquier proceso vivo del sistema "se solapa" con cualquier rango de `delegated_cpus`, sin que eso implique que esté causando contención real de caché/ancho de banda en este instante. `Cpus_allowed` responde "¿podría estar acá?", no "¿está acá ahora, compitiendo por el mismo core físico?" — la pregunta que realmente le importa a la contención de L3/memoria. Corregido: `detect_foreign_affinity_pids()` ahora lee el campo `processor` de `/proc/<pid>/stat` (qué CPU tiene asignado el proceso en este instante, campo 39 según `man proc`, ubicado con cuidado porque el campo `comm` puede contener espacios/paréntesis) junto con `state` (campo 3); solo cuenta como ajeno un proceso con `state=='R'` (corriendo, no dormido) **y** `processor` dentro de `delegated_cpus` — una foto del scheduler en este instante, no una posibilidad teórica. Contra el `/proc` real de esta máquina de desarrollo: la versión por `Cpus_allowed` marcaba 260 procesos como "ajenos" en 8 CPUs sin ningún pinning; la versión por `processor`+`state` marca 0-1 (solo lo que de verdad está ejecutando ciclos en ese instante). 7 tests actualizados/nuevos, incluido uno que verifica que un proceso dormido con `processor` coincidente NO cuenta como ajeno (el caso exacto que causó el rechazo masivo), y uno para el parseo robusto de `comm` con espacios/paréntesis. | E06, preflight.py, campaign.py. | Corregido y verificado contra hardware real (siguiente paso: re-correr el piloto). |
| ARC-45 (2026-08-01) | **F4.4 cerrado: primera campaña real completa de punta a punta en felix**, `campaign_felix_ref.yaml` (npb\_ep + npb\_mg × REF × 3 repeticiones), después de corregir ARC-42/43/44. Resultado: **6/6 combinaciones aceptadas, 0 rechazadas**, `windows.csv` real generado por primera vez por el pipeline completo (antes solo existía contra mocks/fakes en tests). Señal físicamente coherente: `npb_ep` (5/6450 ventanas `ok`, el resto `intensity_undefined`) es el comportamiento *esperado* de un kernel que casi no toca memoria (`bytes_moved_window≈0` en casi toda la corrida → intensidad operacional indefinida, no un bug — las 5 ventanas válidas clasifican `compute_bound`, coincidiendo con `phase_label_hint`); `npb_mg` (2875/3177 `ok`, mayormente `memory_bound` con una mezcla real de `compute_bound`, consistente con las distintas fases de un multigrid) también es plausible. Hallazgos menores, no bloqueantes, para revisión futura: (1) advertencia D04 (`calibration_references cv_pct=56.99%` sobre el umbral de 5%, no bloqueante) — no investigada a fondo, candidata a revisión antes de F4.5; (2) la calibración (`stream_official`/`ert_probe`) corrió con 8 hilos (`OPENMP_THREADS 8` en su stdout) en vez de los 6 `delegated_cpus` reales de la campaña — el orquestador no fija `OMP_NUM_THREADS` para igualar el conteo real de cores medidos, así que `i_ridge` de esta corrida refleja un pico a 8 hilos, no a 6; (3) el CLI (`cmd_run_campaign`) sigue sin invocar `run_campaign_preflight()` automáticamente (ya documentado como brecha en el hallazgo de F4.2) — el preflight completo se corrió a mano por separado antes de lanzar la campaña, no como parte del comando real. F4.5 (gate INT-T08 sobre una campaña piloto extendida) queda como siguiente paso natural. | F4.4, campaign\_felix\_ref.yaml. | Piloto mínimo completo y verificado en hardware real. Hallazgos menores documentados, no resueltos en este cambio. |
| ARC-46 (2026-08-05) | Diagnóstico del hallazgo (1) de ARC-45 (advertencia D04). Se recalculó `cv_pct` a mano por métrica a partir de los contadores finales reales de las 5 repeticiones de calibración de `npb_ep`: `ipc` (CV 0.09%) e `ips` (CV 0.90%) están perfectamente estables; `mpki` (CV 55.83%) y `miss_rate` (CV 56.99%, el que dispara D04) no. Causa: los `cache_misses` absolutos de esas 5 corridas fueron `979, 1581, 1848, 2220, 4220` — sobre ~123 mil millones de instrucciones cada una. Con un conteo tan chico, cualquier variación normal del contador (un par de page faults más o menos, jitter de TLB) mueve el número relativo muchísimo; es la firma de medir algo en el piso de ruido del contador, no evidencia de contaminación ni de un bug del pipeline (`ipc`/`ips`, las métricas que sí importan para EP, confirman que la medición en sí es sólida). Conclusión: `npb_ep` es la peor elección posible como `reference_kernel_ref` para CAL-09/10 precisamente porque esa calibración exige estabilidad en las 4 métricas, y `mpki`/`miss_rate` nunca van a ser estables en un kernel que casi no genera cache-misses (el mismo hallazgo, en otras palabras, que explica por qué el 99.9% de las ventanas de EP salen `intensity_undefined` en `windows.csv`, ARC-45). Se cambió `reference_kernel_ref` a `npb_mg` (tráfico de memoria real y medible) en el nuevo manifest `campaign_felix_ref_full.yaml`, que además amplía la campaña a los 7 kernels del catálogo completo (antes solo 2, el mínimo de F4.4) — pedido explícito del usuario ("hagamos una prueba ya real con lo que tenemos"). | D04, CAL-09, CAL-10, campaign\_felix\_ref\_full.yaml. | Diagnosticado y corregido; pendiente confirmar en la corrida real que `npb_mg` sí da `cv_pct` bajo el umbral. |
| ARC-47 (2026-08-05) | **Campaña real con el catálogo completo (7 kernels × REF × 3 repeticiones) corrida contra felix.** Resultado: **21/21 combinaciones aceptadas, 0 rechazadas**, `total_core_hours≈1.0`. Confirma el diagnóstico de ARC-46: `calibration_references` con `reference_kernel_ref=npb_mg` da `cv_pct=0.82%` (antes 56.99% con `npb_ep`), `accepted=true`. Señal de `phase_label_train` en `windows.csv` coherente con `phase_label_hint` en los 7 kernels: `npb_ep` 8/8 (100%) y `dgemm_n2048` 11862/11912 (99.6%) compute\_bound; `npb_cg` 24785/24791 (99.98%) y `npb_mg` 2677/2890 (92.6%) memory\_bound; `npb_is` 70.5% memory\_bound (mezclado, mayoría correcta); `npb_ft` (74.9%/25.1%) y `npb_lu` (68.6%/31.4%) muestran mezcla real compute/memory, coherente con su hint `intermedio` (no deberían separar limpio). Esto es, informalmente, una confirmación del criterio de INT-T08 (F4.5) sin haberlo corrido todavía como gate formal con el umbral ≥80% que propone el plan. Nota operativa: el comando `srun` se lanzó como tarea en segundo plano (duración estimada ~10-15 min); la sesión SSH que lo esperaba se colgó silenciosamente sin entregar la salida final (mismo tipo de corte de red intermitente ya visto con la VPN de la universidad), pero el job de Slurm sí terminó normalmente del lado del clúster — se verificó con `squeue` (vacío) y leyendo los artefactos directamente por una conexión nueva, sin perder ningún resultado. | F4.4, F4.5 (informal), CAL-09, CAL-10, campaign\_felix\_ref\_full.yaml. | Verificado en hardware real. INT-T08 como gate formal (umbral ≥80%) queda para cuando se decida congelar un dataset candidato. |
| ARC-48 (2026-08-05) | **Corrección crítica a ARC-47: los números de "21/21 aceptadas" ocultaban que 14 de esas 21 corridas (todas las repeticiones 2 y 3 de los 7 kernels) tenían `windows.csv` COMPLETAMENTE VACÍO** (solo encabezado, cero filas de datos) — confirmado con `wc -l` sobre los 21 archivos. `verdict.json` las marcaba `accepted:true` igual, porque la validación (checksum, exit code, success\_check) nunca revisa el contenido de `windows.csv`. Las cifras de `phase_label_train` reportadas en ARC-47 siguen siendo válidas (se calcularon explícitamente sobre `rep01`, la única con datos), pero la lectura "21/21 aceptadas = todo funciona" era engañosa: solo 7 de 21 corridas (33%) tenían datos utilizables. Causa raíz: `runner.py` nunca pasa `--repetitions` al launcher C++, así que cada invocación usa el default (`opt.repetitions=1`) y su columna interna `repetition` en `samples.csv` **siempre vale "1"**, sin importar si la corrida es la repetición 1, 2 o 3 a nivel de campaña. `postprocess.build_windows()` filtraba `samples.csv` por `context.repetition` (el índice de repetición de la CAMPAÑA, 1/2/3) en vez de por la numeración interna del launcher (siempre 1) — para `repetition_index>=2` el filtro nunca encontraba ninguna fila y `build_windows()` devolvía una lista vacía en silencio, sin excepción ni advertencia. Los datos crudos (`samples.csv`) estaban completos y correctos en las 21 corridas (confirmado, `samples_collected` en `metadata.json` coincide exactamente con las líneas de `samples.csv`) — el bug es puramente de post-procesamiento, no de medición: el harness C++/collector nunca perdió ni un dato. Ningún test lo detectó porque nunca se había corrido una campaña real con `repetitions_per_combination>1` contra datos reales; los tests de `postprocess.py` siempre usaron `context.repetition=1` junto con filas de CSV con `repetition=1`, ocultando el desacople entre ambos conceptos. Corregido: `_split_by_repetition_and_tag()` ahora filtra por una constante `_LAUNCHER_INTERNAL_REPETITION=1` (documentada), nunca por `context.repetition` — `context.repetition` sigue usándose exclusivamente como metadata de salida (la columna `repetition` de `windows.csv`), nunca como filtro de entrada. Test de regresión nuevo con el escenario exacto (`context.repetition=2`, CSV con `repetition=1`). **Pendiente:** re-correr la campaña completa con el fix para tener datos reales de las 21 corridas antes de cualquier análisis de F4.5/INT-T08. | POST-01..16, RUN-06, postprocess.py. | Corregido y verificado con test de regresión; pendiente re-correr la campaña real para reemplazar los datos incompletos de ARC-47. |
| ARC-49 (2026-08-05) | **Campaña real re-corrida con el fix de ARC-48. Las 21 corridas (7 kernels × REF × 3 repeticiones) tienen ahora `windows.csv` con datos reales** (~305 554 filas totales, antes ~101 928 con 14/21 vacíos). `campaign_metadata.json`: 21 aceptadas, 0 rechazadas. `calibration_references` con `npb_mg`: `cv_pct=0.88%` (confirma ARC-46 de nuevo, con datos frescos). Indicadores de salud del harness en las 21 corridas: `perf_running_ratio_min=1.0000` (nunca multiplexó), `push_retries=0` (nunca se llenó el ring buffer), `sampling_interval_cv_pct` entre 0.43% y 11.16% (media 2.42%). `phase_label_train` agregado sobre las 3 repeticiones de cada kernel coincide con `phase_label_hint`: `npb_ep` 100%, `dgemm_n2048` 99.6%, `npb_cg` 100.0%, `npb_mg` 92.8% en la dirección esperada; `npb_is` mayoritariamente correcto (69.9%); `npb_ft`/`npb_lu` (hint `intermedio`) muestran mezcla real 68-75%/25-32% compute/memory, coherente con no deber separar limpio. Diagnóstico completo (mecánica del orquestador + sentido físico de los datos + los 6 bugs de ARC-42..48 + brechas abiertas + recomendación priorizada dado el cronograma de 3 semanas del proyecto) en `docs/retoma/Informe_Diagnostico_Beta_2026-08-05.md`, pedido explícito del usuario por la urgencia del proyecto. Hallazgo central del informe: el pipeline funciona correctamente, pero **H1 (permiso de escritura cpufreq) sigue sin respuesta de SC3** y es el riesgo más urgente del cronograma — todo lo verificado hasta ahora es a una sola frecuencia (REF), no hay todavía ningún dato real de DVFS multi-nivel. | F4.4, F4.5 (informal), Informe\_Diagnostico\_Beta\_2026-08-05.md. | Campaña real completa y correcta verificada en hardware. Diagnóstico entregado; H1 identificado como bloqueador crítico de cronograma, no de código. |
| ARC-33 (2026-07-31) | F3.4 (validación de bytes) ejecutada en felix: harness C++ compilado con cmake+gnu14 dentro de un `srun` (satisface también la mitad de F4.1: los 10 CTest del harness pasan en el nodo real). Se corrió `telemetry_kernel_launcher --exec stream_c` con `delegated_cpus=0-5, collector_cpu=6, consumer_cpu=7` (esquema exacto de F4.2), `--interval-ns 1000000`. Resultado: `Σ delta_cache_misses × 64 = 33.9 GB` observado vs `51.2 GB` teórico (NTIMES=10 × 10 arreglos-toque × STREAM_ARRAY_SIZE × 8 bytes, fórmula propia de `stream.c`) → **-33.8%, fuera del criterio ±30% de F3.4 por poco**. Se investigó la causa antes de decidir una acción: (1) `perf stat` nativo sobre el mismo binario dio `cache-misses=548.9M` (vs `529.5M` de nuestro harness, diferencia de solo 3.5% — **el collector PID+inherit está midiendo correctamente**, no hay evidencia de que el mecanismo de herencia subcuente hilos OpenMP); (2) desglosar en `LLC-load-misses + LLC-store-misses` (351.5M + 201.5M = 553.0M) da un total prácticamente idéntico al evento genérico `cache-misses` — descarta que el evento genérico esté eligiendo una definición más estrecha que las métricas LLC-específicas que expone felix. Conclusión: el déficit es real y consistente entre tres mediciones independientes, no un artefacto de medición; la explicación más plausible es el prefetcher de hardware L2 (streaming, muy agresivo en Nehalem-EX) convirtiendo una fracción de los accesos secuenciales de STREAM en hits de L1/L2 antes de que el contador de "LLC miss" los vea, aunque el dato sí haya viajado por el bus de memoria — exactamente el riesgo que el propio plan anticipó en F3.4. Como evidencia adicional de que el collector funciona correctamente (más allá del recuento de bytes): IPC promedio de la corrida = 0.184, tasa de miss = 95.7% de cache-references, MPKI = 55 — firma textbook de un kernel memory-bound, coherente con STREAM y con el hint del catálogo. `sampling_interval_cv_pct=3.04%`, `perf_running_ratio_min=1.0`, `push_retries=0` en los 5705 samples — el muestreo a 1ms es preciso y sin pérdida de datos ni multiplexado de PMCs. Decisión: **no bloquear F4** por este resultado — se documenta como sesgo sistemático conocido y cuantificado (bytes_moved_window subestima el tráfico real de memoria en cargas altamente prefetch-friendly como STREAM en aproximadamente 30-34%; el sesgo puede diferir para kernels con patrones de acceso menos regulares como CG/IS, sin cuantificar todavía). No cambia la clasificación relativa compute-bound vs memory-bound (que depende de la posición relativa en el espacio IPC/intensidad, no del valor absoluto de bytes), pero sí afecta cualquier lectura absoluta de `operational_intensity`/EDP. Ver informe completo en `docs/retoma/Informe_Piloto_F3_2026-07-31.md`. | POST-10, F3.4, F4.1 (parcial), collector.cpp, perf_reader.cpp. | Verificado en hardware; hallazgo documentado, no bloqueante. Ground truth de bytes queda con sesgo conocido, no resuelto — candidato a revisión futura con contadores uncore/IMC si se necesita precisión absoluta. |
| ARC-69 (2026-08-06) | **`nvml_reader.cpp` nunca compiló contra un header NVML real: usaba `nvmlUtilizationRates_t`, un tipo que no existe en la API real (el tipo correcto es `nvmlUtilization_t`) -- bug de nacimiento en el código GPU del harness, invisible desde siempre porque `WITH_GPU=ON` nunca se había compilado contra un `nvml.h` real hasta hoy.** Encontrado al llevar ARC-66/68 a `paccaA100` real para probarlos con hardware de verdad: el stub local de NVML que se usó para verificar sin GPU (ARC-66, ARC-68) **repitió el mismo nombre inventado de memoria** (`nvmlUtilizationRates_t`), así que compilaba contra sí mismo sin detectar el error -- el stub daba falsa confianza exactamente en el punto donde más importaba. Confirmado leyendo el header real en pacca (`.../cuda/12.0/.../include/nvml.h`, CUDA 12.0): `nvmlDeviceGetUtilizationRates(nvmlDevice_t, nvmlUtilization_t*)`, struct `nvmlUtilization_st` con los mismos campos (`gpu`, `memory`) que ya se usaban -- el fix es solo el nombre del tipo, no la lógica. **Verificado de punta a punta en hardware real, no solo que compile:** se armó un directorio aislado nuevo (`~/hyperion-gpu-verify`, fuera del checkout `~/hyperion` real que tiene trabajo sin commitear de otra sesión) copiando solo `telemetry/`, se compiló con `WITH_GPU=ON` contra el NVML real de pacca (`nvml.h` de CUDA 12.0 + el stub de enlace `libnvidia-ml.so` del toolkit, que resuelve en runtime contra `libnvidia-ml.so.1` real vía su SONAME) -- **11/11 tests C++ en verde con NVML real** (antes 11/11 solo con el stub local). Se corrió además `telemetry_kernel_launcher --exec /bin/sleep 1.0 --enable-gpu --gpu-interval-ns 100000000` de punta a punta: 10 filas GPU en ~1s con separación real de ~100ms entre timestamps, potencia real ~36.3W (coherente con el reposo de la A100 ya visto con `nvidia-smi` en ARC-59/62), `util_pct=0` correcto porque `/bin/sleep` no toca la GPU. Directorio de prueba y snapshot descartados al terminar, no se comprometió nada al repo del cluster. **Lección para el proceso:** un stub escrito de memoria puede ocultar exactamente el tipo de error que está diseñado para atrapar, si el error está en el propio nombre que el stub reproduce -- la verificación contra el header/hardware real sigue siendo la única prueba que cuenta, el stub solo sirve para no gastar una vuelta al cluster en errores de sintaxis triviales. | telemetry/src/nvml\_reader.cpp, ARC-66, ARC-68. | **Corregido y verificado en hardware real (paccaA100, NVML real, no un stub).** Bug de código real encontrado y arreglado, no solo una limitación de las pruebas. |
| ARC-68 (2026-08-06) | **`telemetry_kernel_launcher` gana `--enable-gpu`/`--gpu-interval-ns`: la clase `Collector` ya soportaba muestreo de GPU (`enable_gpu`, ARC-66), pero el binario real usado en producción no tenía forma de pedirlo -- `enable_gpu` nunca se conectaba a ningún flag de CLI, hallazgo del usuario al preguntar si ya quedaba listo para probar en pacca.** Cierra ese hueco: `Options` gana `enable_gpu`/`gpu_interval_ns`, `parse_args()` los parsea (`--enable-gpu` flag booleano, `--gpu-interval-ns <ns>` opcional, valida que sea positivo si se activa GPU), se pasan a `CollectorConfig`, y se agregan a `metadata.json` para trazabilidad (mismo patrón que `enable_perf`). `usage()` actualizado. **Verificado end-to-end, no solo que compile:** se armó un stub local mínimo de NVML (mismo mecanismo que ARC-66, descartado después) para compilar con `WITH_GPU=ON` y correr el binario real de punta a punta (`--exec /bin/sleep 0.5 --enable-gpu --gpu-interval-ns 100000000`) -- `samples.csv` mostró exactamente 6 filas GPU en ~0.5s (cadencia real de 100ms confirmada por los timestamps, no las ~500 que darían un muestreo a 1ms) y `metadata.json` refleja `enable_gpu=true`/`gpu_interval_ns=100000000`. Suite completa verificada en las dos configuraciones locales (12 tests sin GPU, 11 con `WITH_GPU=ON` vía stub, ambas en verde) más 207 tests Python. Intento de llevar esto a `paccaA100` real: el checkout `~/hyperion` de pacca está muy atrás en el historial (`HEAD` en `7233ab6`, antes de ARC-53) y tiene cambios locales sin commitear de otra sesión concurrente en archivos que este cambio también toca (`collector.hpp`, `telemetry_kernel_launcher.cpp`, `perf_reader.*`) -- **no se tocó ese checkout** para no arriesgar ese trabajo en curso; en vez de eso se preparó un directorio aislado nuevo para probar contra el NVML/CUDA real del nodo sin interferir. | telemetry/experiments/telemetry\_kernel\_launcher.cpp (Options, parse\_args, usage, metadata.json), ARC-66. | **Implementado y verificado end-to-end con hardware sintético (stub NVML) en dos configuraciones locales.** Verificación contra NVML real de `paccaA100` en curso, en un checkout aislado para no interferir con el trabajo sin commitear de otra sesión en `~/hyperion`. |
| ARC-67 (2026-08-06) | **Aclaración en el documento de diseño GPU: el diagrama "dos loops independientes" es el daemon de Fase 3 (Python, sin construir), no el colector C++ de Fase 1 que ya existe.** A pedido del usuario, tras implementar ARC-66, para que quedara explícito por qué el fix de `gpu_interval_ns` terminó siendo una compuerta de tiempo dentro de un solo hilo y no un segundo hilo/loop real: el colector (`telemetry/src/collector.cpp`) tiene un único hilo productor porque `SPSCRing` es estrictamente de un solo productor (`spsc_ring.hpp`) -- esa restricción de hardware/diseño no aplica al daemon de Fase 3, que solo consume filas ya escritas por el colector y sí puede tener dos hilos/procesos genuinamente independientes para CPU y GPU. Sección 4.0 nueva en `Diseno_Politica_DVFS_CPU_GPU.md`, justo antes de 4.1, para que nadie lea el diagrama de la sección 4 y asuma que ya existe (o que se puede construir tal cual) un segundo hilo productor en el colector actual. Sin cambios de código. | docs/retoma/pacca/Diseno\_Politica\_DVFS\_CPU\_GPU.md (sección 4.0, nueva), ARC-66. | **Aclaración documental, sin cambios de código.** |
| ARC-66 (2026-08-06) | **Ejecución del plan de ARC-65: refactor de `GpuClockController` a la firma correcta, y saneamiento del muestreo NVML fuera del tick de 1 ms -- ambos implementados y probados, no solo diseñados.** (1) `gpu_clock_controller.hpp`: `on_phase_begin()` ya no recibe una intensidad ni clasifica internamente contra `i_ridge` -- recibe `GpuPhaseLabel` ya decidido (por `ncu` en Fase 1 o por el modelo en el daemon, según ARC-65), y solo conserva la máquina de histéresis/dwell. Test reescrito a la nueva firma. (2) `collector.cpp`/`.hpp`: nuevo `CollectorConfig::gpu_interval_ns` (100ms por defecto) gatea la lectura de NVML dentro del mismo hilo productor -- **no se pudo usar un segundo hilo/ring** porque `SPSCRing` es estrictamente de un solo productor (confirmado leyendo `spsc_ring.hpp` antes de intentar nada, para no violar esa garantía a ciegas); la solución real es una compuerta de tiempo (`next_gpu_sample_ns_`) dentro del mismo loop de 1 ms, mucho más simple que la arquitectura de "loop GPU separado" que ARC-65 dejaba como aspiración. Test nuevo `test_collector_gpu_cadence.cpp` instancia un `Collector` real con GPU habilitada y confirma **empíricamente** (no por inspección de código) que en 300ms con `gpu_interval_ns=50ms` se reciben ~6 muestras de GPU y no ~300 -- se salta (exit 77) en builds sin `TELEMETRY_WITH_GPU`, mismo patrón que `perf_reader_pid_live_test`. Para poder probar la rama `#ifdef TELEMETRY_WITH_GPU` en esta máquina de desarrollo (sin GPU ni drivers NVIDIA) se armó un stub local mínimo de `nvml.h`/`libnvidia-ml.so` (símbolos justos para compilar y devolver valores fijos, nunca until GPU real) -- usado solo para verificar localmente antes de tocar el nodo real, descartado después, no comprometido al repo. Verificado en dos configuraciones: build normal (12 tests, el nuevo se saltea) y build con `WITH_GPU=ON` contra el stub (11/11 en verde, incluida la prueba de cadencia real). Suite completa del proyecto (Python + C++) sigue en verde. Explícitamente fuera de alcance de este cambio (dependen de decisiones o recursos que no estaban disponibles hoy): el manifiesto de la campaña de caracterización GPU (depende de elegir los kernels Rodinia concretos) y el mecanismo para `cudaDeviceScheduleBlockingSync` sobre binarios de terceros (hipótesis de shim `LD_PRELOAD` anotada, no implementada ni probada). | telemetry/include/telemetry/gpu\_clock\_controller.hpp, telemetry/tests/test\_gpu\_clock\_controller.cpp, telemetry/include/telemetry/collector.hpp, telemetry/src/collector.cpp, telemetry/tests/test\_collector\_gpu\_cadence.cpp (nuevo), telemetry/CMakeLists.txt, docs/retoma/pacca/Diseno\_Politica\_DVFS\_CPU\_GPU.md (sección 8/9 actualizada), ARC-65. | **Implementado y verificado con tests reales** (12 C++ sin GPU + 11 C++ con GPU vía stub local + 207 Python), no solo diseñado. Sin hardware GPU real todavía -- el stub confirma que el código compila y se comporta como se espera, no reemplaza la verificación final en `paccaA100`. |
| ARC-65 (2026-08-06) | **Reescritura completa del diseño de la política DVFS de GPU: la versión anterior (ARC-62/64) contradecía el plan de trabajo de grado aprobado, hallazgo del usuario al pedir explícitamente verificar contra `docs/general/plan_trabajo_grado.md` como "verdad absoluta, no se puede contradecir".** Tres errores concretos encontrados y corregidos: (1) el diseño anterior proponía que GPU funcionara con una **tabla estática kernel→etiqueta** poblada offline con `ncu`, sin modelo entrenado ni inferencia en vivo -- contradice literalmente la sección 5.1 del plan (la telemetría de GPU en Fase 1 es "utilización de SM, uso de memoria y consumo de potencia" vía NVML, sin mencionar `ncu`/FLOPs/bytes) y la 5.3 (el daemon "en cada instante... construye el vector de entrada para el modelo y ejecuta la inferencia", también para GPU). Una tabla además no generaliza a kernels no vistos, objeción propia del usuario que resultó ser la misma razón por la que el plan exige un modelo entrenado y no un lookup. (2) el diseño anterior asumía "código propio" para los benchmarks GPU y proponía llamar directo al motor de decisión en el punto de lanzamiento del kernel -- corregido tras precisión del usuario: los kernels son de **Rodinia (terceros)**, no código del proyecto, mismo principio que ya aplica NPB en CPU (no se modifica el binario, se mide desde afuera). (3) Como consecuencia de (1), la necesidad de CUPTI/inyección para detectar límites de kernel **desaparece por completo**: si el modelo de GPU predice directo desde el vector NVML actual (igual que el de CPU predice desde el vector Perf actual, sin saber qué kernel de NPB corre), nunca hace falta saber "cuál kernel específico está corriendo". Solución reconciliada: `ncu` pasa a jugar exactamente el rol que `bytes_moved_window`/`flops_window_estimate` ya juegan del lado CPU -- **etiquetador de verdad (ground truth) para Fase 1, corrido una sola vez por kernel** (la intensidad no depende de la frecuencia), nunca invocado en producción; las muestras NVML recolectadas en la campaña de caracterización (a través de los niveles FG0-FG4, sección 6 del documento) son las *features* reales de entrenamiento, etiquetadas con el resultado de `ncu`. Se aclaró además, a pedido explícito del usuario, que la salida sigue siendo binaria (`compute_bound`/`memory_bound`) y NO se convierte en 4 clases -- los "cuatro escenarios base" del plan (CPU/GPU × compute/memory) son un criterio de **muestreo estratificado** para el dataset, no el espacio de salida (confirmado releyendo 5.2: "el modelo... produce como salida una etiqueta... cómputo o memoria", dos clases, sin dispositivo); la arquitectura correcta son **dos clasificadores binarios independientes**, uno por dispositivo (features disjuntas: IPC/miss-rate de Perf vs util/potencia de NVML), corriendo en dos loops desacoplados dentro de un mismo proceso daemon, coordinados solo por la señal "GPU ocupada ⟹ CPU a mínimo" -- documentado con precisión el caso spin-wait vs blocking-sync (en bloqueo real la frecuencia casi no afecta el consumo porque no hay conmutación que escale con `f`, pero forzar el mínimo sigue siendo correcto como medida defensiva sin costo, no como corrección de un error de clasificación como en el caso de spin activo). Documento completamente reescrito (`Diseno_Politica_DVFS_CPU_GPU.md`, versión 2) con una sección 0 que cita textualmente las cuatro restricciones del plan antes de diseñar nada más. Pendiente explícito, no resuelto en este cambio: refactorizar `GpuClockController::on_phase_begin()` para recibir la etiqueta ya decidida en vez de clasificar internamente contra `i_ridge` (su lógica de histéresis/dwell sigue siendo válida, solo cambia qué recibe como entrada) -- marcado deliberadamente como "no hacer sin avisar" en el documento, no se tocó el código en este cambio. | docs/retoma/pacca/Diseno\_Politica\_DVFS\_CPU\_GPU.md (reescrito completo), docs/general/plan\_trabajo\_grado.md (fuente de verdad, no modificado), ARC-62, ARC-64. | **Corregido a nivel de diseño, verificado contra el plan aprobado línea por línea.** Sin cambios de código todavía -- el refactor de `gpu_clock_controller.hpp` queda pendiente y documentado explícitamente como tal. |
| ARC-64 (2026-08-06) | **Primer código real de la política DVFS de GPU: motor de decisión con histéresis y permanencia mínima (`GpuClockController`), separado del harness de CPU por diseño, no como parche.** A pedido explícito del usuario tras discutir ARC-62: confirmó que el permiso de reloj de GPU (P4) sí va a llegar, pero insistió en no quedar atados a cómo se construyó el harness (pensado sólo para CPU, un único loop de muestreo a 1 ms + una única tabla de ventanas) — hay que replantear la arquitectura para GPU, no forzarla al mismo esquema. Decisión de arquitectura: **dos dominios de control desacoplados** (CPU sigue igual, decisión por ventana de 1 ms; GPU decide por *fase* -- una corrida maximal de kernels con la misma clasificación -- porque cambiar su reloj es caro y decidir cada 1 ms desperdiciaría más overhead de transición del que ahorra), coordinados por una sola señal compartida ("GPU ocupada" gatea el reloj de CPU a mínimo). Mecanismo de límite de fase elegido: **llamada directa en el punto de lanzamiento dentro del mismo proceso** (los benchmarks GPU del proyecto son código propio, a diferencia de los binarios NPB opacos de CPU), no un colector externo ni inyección CUPTI -- eso queda anotado como alternativa futura sólo si hiciera falta medir binarios de terceros. Implementado: `telemetry/include/telemetry/gpu_clock_controller.hpp`, motor de decisión puro (clasifica intensidad estática de la fase contra `i_ridge` con banda de histéresis configurable, aplica un piso `min_dwell_ns` antes de permitir un nuevo cambio de reloj, nunca cree que un cambio ocurrió si el `ClockSetter` inyectado devuelve falla) que **no depende de NVML/CUDA en absoluto** -- el cambio de reloj real se inyecta como `std::function<bool(unsigned int)>`, mismo patrón que `campaign.py` inyectando `apply_frequency()` en vez de llamar a `freqctl` directo, para poder testear toda la lógica sin GPU. Test nuevo `test_gpu_clock_controller.cpp` cubre: primera aplicación incondicional, banda de histéresis que evita reclasificar, supresión por dwell insuficiente, aplicación al superar el dwell, y que un `ClockSetter` que falla no corrompe el estado interno. 11/11 tests C++ en verde (10 previos + 1 nuevo), compilado y corrido en un build local aislado (`telemetry/build-gpuctl`, sin `WITH_GPU`, descartado tras verificar). Deliberadamente NO implementado hoy (fuera de alcance de "listo hoy", documentado como pendiente): el wrapper real a `nvmlDeviceSetGpuLockedClocks` (bloqueado por P4 sin permiso para probarlo en hardware), y la tabla de intensidad estática por kernel (depende de que existan los benchmarks GPU del catálogo, todavía no escritos, y de correrles `ncu` -- ya confirmado que funciona sin permisos, ARC-62). Diseño completo con la secuencia de qué falta y en qué orden en `docs/retoma/pacca/Diseno_Politica_DVFS_CPU_GPU.md` sección 3.6. | telemetry/include/telemetry/gpu\_clock\_controller.hpp (nuevo), telemetry/tests/test\_gpu\_clock\_controller.cpp (nuevo), telemetry/CMakeLists.txt, docs/retoma/pacca/Diseno\_Politica\_DVFS\_CPU\_GPU.md (sección 3.6, nueva), ARC-62. | **Implementado y verificado con tests (11/11 C++), sin hardware real todavía** -- el motor de decisión es independiente de NVML por diseño, así que no requiere GPU para probarse; lo que sí requiere GPU y permiso (`nvmlDeviceSetGpuLockedClocks` real, `T_transición` medido) queda explícitamente pendiente y listado en orden. |
| ARC-62 (2026-08-06) | **Auditoría de capacidades GPU del A100 antes de comprometer el diseño de la política DVFS heterogénea: el control de reloj de GPU está bloqueado por permisos igual que el de CPU, pero la medición (profiling) está abierta -- lo que invierte la estrategia prevista.** Cuatro hallazgos verificados en `paccaA100` real (consultas de solo lectura vía `srun`, más un kernel CUDA propio trivial para el test de profiling): (1) **`Applications Clocks` está DEPRECADO en el driver 595.45.04** (`nvidia-smi -q -d CLOCK` responde literalmente `Requested functionality has been deprecated`) -- la vía histórica por la que un usuario sin privilegios podía fijar relojes de GPU (`nvidia-smi --applications-clocks-permission=UNRESTRICTED`) **ya no existe en este driver**; sólo queda `nvmlDeviceSetGpuLockedClocks`/`nvidia-smi -lgc`, que exige root. Consecuencia estratégica importante: **la GPU NO es un hedge contra el bloqueo de `scaling_min_freq` en CPU** (hipótesis que se barajó), cae en la misma clase de permiso -- el correo de solicitud, que sigue sin enviarse, pasa a ser camino crítico de dos entregables en vez de uno. (2) **`ncu` (Nsight Compute) funciona sin privilegios en este nodo** -- verificado compilando y perfilando un axpy propio de 2²⁴ doubles: `dram__bytes.sum=388.80 MB` contra ≈402 MB analíticos, coherente. En muchas instalaciones el profiling de GPU está restringido por `NVreg_RestrictProfilingToAdminUsers`; aquí no lo está, así que **la caracterización real de FLOPs/bytes por kernel de GPU está disponible hoy sin pedir nada** -- es la pieza que faltaba para poder calcular intensidad operacional de GPU, y no requiere CUPTI. (3) **El espacio DVFS de GPU es unidimensional**: `SUPPORTED_CLOCKS` lista 81 valores de reloj de SM (765-1410 MHz) y **un solo valor de reloj de memoria** (1215 MHz, no ajustable) -- no hay segundo eje que explorar en la matriz experimental de GPU. (4) **DCGM no está instalado** (`dcgmi`/`nv-hostengine` ausentes), así que la vía de métricas de profiling continuas (`DCGM_FI_PROF_*`) requeriría instalación por parte del administrador; no conviene diseñar asumiéndola. Hallazgo adicional de arquitectura encontrado revisando el código con estos datos en mano: **la clasificación Roofline de GPU tal como estaba planteada no es implementable con el harness actual** -- `GpuSample` (`metrics.hpp`) sólo tiene `power_mw`/`util_pct`, ninguno de los cuales es numerador ni denominador de intensidad operacional, y `postprocess.py` **descarta las filas GPU por completo** (no hay ninguna columna GPU en `REQUIRED_OUTPUT_COLUMNS`; el launcher las escribe a `samples.csv` y nadie las consume). Respuesta de diseño completa, con las cinco preguntas del prompt contestadas y un plan priorizado, en `docs/retoma/pacca/Diseno_Politica_DVFS_CPU_GPU.md` (recomendación central: intensidad de GPU por caracterización estática offline con `ncu` guardada en el catálogo, NVML de runtime sólo para atribución/segmentación y energía, dos políticas independientes con una regla de coordinación en vez de un optimizador conjunto). `Solicitud_Permisos_Pacca_Unicartagena.md` gana un cuarto pedido (P4, control de reloj de GPU) y el borrador de correo se actualizó a cuatro ítems; también se agregó a "Resueltos, no requieren solicitud" que `ncu` ya funciona sin privilegios, para que la administración sepa qué NO tiene que tocar. Sin cambios de código en este ARC -- es auditoría y decisión de arquitectura. | docs/retoma/pacca/Diseno\_Politica\_DVFS\_CPU\_GPU.md (nuevo), docs/retoma/pacca/Solicitud\_Permisos\_Pacca\_Unicartagena.md (P4 + correo + resueltos), docs/retoma/pacca/Prompt\_Diseno\_Politica\_DVFS\_CPU\_GPU.md (documento que originó esta respuesta), telemetry/include/telemetry/metrics.hpp y orchestrator/postprocess.py (diagnosticados, no modificados). | **Auditado y verificado en hardware real.** Decisión de arquitectura tomada y documentada; implementación pendiente. Riesgos de código identificados y NO corregidos todavía (spin-wait de `cudaDeviceSynchronize` que hace ver `compute_bound` a un CPU ocioso; NVML muestreado dentro del loop de 1 ms degradando `sampling_interval_cv_pct`; `power_mw` integrado en vez de usar `nvmlDeviceGetTotalEnergyConsumption()`) -- ver sección 3.5 del documento de diseño. |
| ARC-72 (2026-08-06) | **Fase 1 de GPU completa de punta a punta: selección y compilación de kernels Rodinia reales, shim de blocking-sync verificado en hardware, calibración Roofline de GPU medida, caracterización `ncu` real, e integración completa al catálogo/runner/postprocess -- todo lo necesario para probar en pacca a la frecuencia REF, sin esperar el permiso P4.** A pedido explícito del usuario ("terminar absolutamente todo lo necesario... así sea solo en la frecuencia base de referencia"), acotado deliberadamente a Fase 1 (caracterización), sin tocar modelo ni daemon (Fase 2/3, fuera de alcance). Trabajo, todo verificado en `paccaA100` real: **(1) Kernels Rodinia** -- clonado `yuhc/gpu-rodinia` (mirror con datasets vacíos, `.gitkeep`; inputs generados sintéticamente donde hacía falta). `pathfinder` (candidato inicial memory-bound) descartado al leer su fuente: es enteros puros, sin ningún float -- mismo error que `npb_is`/`npb_ep` (ARC-57), no una intensidad operacional válida. Reemplazado por `backprop` (floats, confirmado). `hotspot` (hint compute-bound de la literatura) compilado y corrido; encontrado y corregido un límite real de CUDA en el camino: el tamaño de entrada de referencia de `backprop` (2097152) excede el límite de 65535 de `gridDim.y` incluso en hardware moderno (el kernel usa `dim3(1, num_blocks)`) -- resuelto con `524288` (`num_blocks=32768`, dentro del límite). **(2) Shim de blocking-sync**: `cudaDeviceSynchronize()` hace spin por defecto; se escribió un probe dedicado (`sync_probe.cu`, no comprometido) que mide el % de CPU en tiempo de usuario durante una espera GPU deliberadamente larga -- confirmado 99.8% (spin) sin el shim, **0.0% (bloqueo real) con el shim vía `LD_PRELOAD`**, sin alterar la salida de `hotspot` ni `backprop` (diff idéntico con/sin shim). El shim (`orchestrator/native/blocking_sync_shim.cpp`) solo necesita el API C de cudart (`cudaSetDeviceFlags`), compila con `g++` normal, no hace falta `nvcc` -- mismo patrón on-demand que `pmc_multiplex_probe.c` (`orchestrator/gpu_shim.py`, ruta de CUDA inferida de dónde está `nvcc` en PATH, nunca adivinada). **(3) Calibración Roofline de GPU real**: BabelStream (`cuda-stream`, Triad ≈1.399 TB/s, ~90% del pico teórico 1555.2 GB/s) + un `cublas_dgemm_bench.cu` nuevo (análogo directo de `dgemm_bench.c`/OpenBLAS, mismo formato de stdout para reusar los mismos regex) dando ≈10.4 TFLOP/s FP64 real -- cuBLAS eligió sorprendentemente un kernel *tensorop* (`cutlass_80_tensorop_d884gemm`), no FP64 vanilla, halazgo relevante para la sección 5 del documento de diseño (qué techo declarar). `i_ridge_gpu` medido ≈7.4 FLOP/byte, consistente con la estimación previa del documento de diseño (~7). **(4) Caracterización `ncu` real** (no asumida de literatura, mismo principio que ARC-57): `hotspot` da 5.03 FLOP/byte -- **por debajo del ridge, memory_bound empíricamente, contradice el hint de "compute-intensive" de la literatura Rodinia** (catalogado como `intermedio`, dejando que la medición hable); `backprop` da 0.087 FLOP/byte, memory_bound confirmado con margen amplio; `cublas_dgemm_bench` (N=4096) da 68.0 FLOP/byte, compute_bound confirmado. **(5) Integración completa al orquestador**: `KernelEntry` gana `device: "cpu"\|"gpu"` (catalog.py, CAT-09 valida el enum); `runner.build_command()` agrega `--enable-gpu`/`--gpu-interval-ns` cuando `device=gpu`; `runner.run_single()` inyecta `LD_PRELOAD`(el shim)/`LD_LIBRARY_PATH` para kernels GPU, degradando a "corre sin el shim" (nunca falla duro) si no hay CUDA en el nodo -- mismo criterio que `stalled_cycles_backend`/`l2_lines_in_all` cuando el nodo no los soporta. **(6) `postprocess.py` deja de descartar las filas GPU**: nuevas columnas `gpu_power_mw`/`gpu_util_pct` en `REQUIRED_OUTPUT_COLUMNS`, nuevo `quality_status=gpu_telemetry` -- las muestras NVML se agregan como passthrough puro (sin ventanear contra límites de CPU, sin calcular intensidad operacional, documentado explícitamente en el código como *features* de entrenamiento para el futuro clasificador de GPU de Fase 2, nunca clasificadas aquí). **5 kernels GPU nuevos en `catalog.yaml`** (`gpu_stream_bw`, `gpu_dgemm_calibration`, `gpu_dgemm_n4096`, `rodinia_hotspot`, `rodinia_backprop`), binarios y checksums reales en `pacca-a100` (felix no tiene GPU). 217/217 tests Python en verde (9 nuevos: `device` en catalog, `--enable-gpu` en `build_command`, `LD_PRELOAD`/`LD_LIBRARY_PATH` en `run_single` con y sin shim disponible, filas GPU passthrough en `postprocess`). Todos los binarios/probes temporales de verificación (`sync_probe.cu`, stubs) se descartaron del cluster al terminar, no se comprometieron al repo. | orchestrator/catalog.py (device), orchestrator/runner.py (build_command, run_single), orchestrator/gpu_shim.py (nuevo), orchestrator/native/blocking_sync_shim.cpp (nuevo), orchestrator/postprocess.py (gpu_telemetry), orchestrator/schemas/kernels/catalog.yaml (5 kernels GPU), kernels/gpu/cublas_dgemm_bench.cu (nuevo), tests/orchestrator/{test_catalog,test_runner,test_postprocess}.py, tests/orchestrator/fixtures/fake_launcher.py, ARC-57, ARC-65, ARC-66, ARC-68. | **Implementado y verificado en hardware real (paccaA100)** en cada pieza por separado (shim, calibración, ncu). Pendiente en el momento de escribir esta entrada: correr una campaña real completa end-to-end con `python -m orchestrator.cli run-campaign` cubriendo estos 5 kernels a REF, para confirmar que toda la cadena integrada (no solo cada pieza por separado) funciona junta. |
| ARC-71 (2026-08-06) | **Análisis profundo cruzando las dos campañas reales del día (clase B `pacca_ref_full_20260806` vs. clase C `pacca_class_c_stress_20260806`, mismos 6 kernels NPB en ambas): dos hallazgos, uno muy positivo y uno que abre una brecha metodológica nueva.** (1) **La clasificación `phase_label_train` es estable entre tamaños de problema para 5 de 6 kernels** (`npb_mg` 99.9%/99.9%, `npb_bt` 85.6%/85.4%, `npb_cg` 92.7%/93.5%, `npb_lu` 88.4%/89.0%, `npb_sp` 58.2%/59.3%, todos con diferencia ≤1.1 puntos porcentuales entre clase B y C) -- evidencia fuerte de que el pipeline mide una propiedad arquitectónica real del kernel, no ruido de una corrida puntual. `npb_ft` es la excepción (79.7%→66.2% compute_bound, sigue dominante pero con mezcla notablemente mayor de memory_bound en clase C), consistente con ser un kernel genuinamente mixto (FFT 3D: mariposas vs. transposición) cuyo balance de fases se desplaza con el tamaño de malla. (2) **El sesgo de `bytes_moved_window` (cross-check contra `L2_LINES_IN_ALL`, ARC-60/63) también depende del tamaño del problema, no solo del kernel** -- `npb_mg` pasa de 30.8% (clase B, el más sesgado) a 3.5% (clase C, uno de los menos); `npb_cg` hace lo opuesto, de 13.5% a **52.4%** (el sesgo más grande medido en todo el proyecto, superando el ~34% de STREAM/felix de F3.4). La caracterización de ARC-33/60/63, hecha a una sola escala, **no se puede extrapolar a otra escala del mismo kernel sin volver a medir** -- refuerza, no resuelve, la necesidad de uncore (bloqueado, ARC-59) como único ground truth independiente del tamaño de problema. Hallazgos secundarios: la potencia media por kernel varía poco entre kernels (114-141 W) y casi nada entre clases del mismo kernel (≤5 W de diferencia) bajo un único nivel de frecuencia (REF) -- refuerza que el permiso de `cpufreq` (bloqueado) es la pieza que falta para ver la relación real frecuencia/tiempo/energía; calibración más estable en clase C (`cv_pct=0.33%` vs `2.27%` en clase B, corridas más largas acumulan más eventos por ventana, mismo mecanismo de ARC-46); y `dgemm_n2048` no acumula ninguna ventana `quality_status=ok` (0/773) porque su `warmup_seconds=0.5` excluye una fracción enorme de su propia duración total (~0.77s) -- clasificación sigue saliendo correcta (no depende del gate de warmup), pero es un ajuste de catálogo pendiente de bajo riesgo. Informe completo en `docs/retoma/pacca/Analisis_Profundo_Piloto_20260806.md`. | docs/retoma/pacca/Analisis_Profundo_Piloto_20260806.md (nuevo), ARC-33, ARC-46, ARC-54, ARC-59, ARC-60, ARC-63. | **Análisis completo, sin cambios de código.** Hallazgo (1) es una validación positiva del pipeline; hallazgo (2) es una brecha metodológica nueva que queda documentada, no resuelta -- depende del mismo bloqueador de permisos de uncore que ARC-59. |
| ARC-63 (2026-08-06) | **`L2_LINES_IN_ALL` instrumentado de forma permanente en el harness C++ real, no solo en un probe externo suelto (ARC-60) -- monitoreo continuo del sesgo de `bytes_moved_window` en cada campaña futura, sin necesitar rearmar la prueba a mano cada vez.** Mismo patrón de degradación elegante que `stalled_cycles_backend` (ARC-50-52): evento crudo (`event=0xF1, umask=0x1F`, sin CMask, ya validado en ARC-60 contra la tabla de LIKWID) abierto solo en family=6/model=106 (Ice Lake-SP), best-effort (nunca aborta el reader si no abre), expuesto vía `PerfReader::has_l2_lines_in_all()`/`Collector::has_l2_lines_in_all()`, columna `l2_lines_in_all` nueva en `samples.csv` (vacía, nunca "0", cuando el nodo no lo soporta -- mismo criterio que ARC-50). `postprocess.py` propaga `delta_l2_lines_in_all` con el mismo gate de `pmu_degraded` condicionado a soporte por nodo, y agrega `bytes_moved_l2_proxy` (mismo multiplicador de tamaño de línea que `bytes_moved_window`, directamente comparable) -- nunca se usa en `operational_intensity`/`phase_label_train`, es puramente una columna de cruce reportada. **Verificado con una campaña real completa en pacca (21/21 aceptadas):** la brecha relativa `bytes_moved_l2_proxy` vs `bytes_moved_window` por kernel confirma y extiende ARC-60 con datos del harness de producción (no un probe aislado) -- `npb_mg` 30.8% (casi idéntico al ~31% de ARC-60), `npb_cg`/`npb_bt`/`npb_sp` 13.3-13.5% (consistente con el 10.8-13.5% de ARC-60), y dos kernels que ARC-60 no había medido: `npb_ft` 17.8%, `npb_lu` 11.3%. Hallazgo nuevo: **`dgemm_n2048` muestra la brecha más grande del catálogo, 45.9%** -- coherente con que OpenBLAS usa *tiling* agresivo (mucho tráfico L2 de reuso entre L1/L2 que `cache_misses`, más aguas abajo, no captura). 10/10 tests C++ en verde (build+CTest reales en pacca), 207/207 tests Python en verde (205 previos + 2 nuevos en `test_postprocess.py`). Sigue siendo un proxy de *núcleo* (L2), no bytes reales de DRAM -- uncore sigue bloqueado (ARC-59), esto no resuelve F3.4/ARC-33, es una segunda señal de cruce permanente mientras esa vía siga cerrada. | telemetry/include/telemetry/perf_reader.hpp, telemetry/include/telemetry/metrics.hpp, telemetry/include/telemetry/collector.hpp, telemetry/src/perf_reader.cpp, telemetry/experiments/telemetry_kernel_launcher.cpp, orchestrator/postprocess.py, tests/orchestrator/test_postprocess.py, ARC-50..52, ARC-59, ARC-60. | **Implementado y verificado en hardware real** con una campaña completa; extiende la caracterización de ARC-60 a los 7 kernels del catálogo actual. |
| ARC-61 (2026-08-06) | **Confirmado que `openblas/0.3.21` en pacca trae headers de desarrollo completos (BLAS+LAPACK C) -- cierra la brecha (8) que quedaba abierta desde la auditoría original ("no confirmado si `-devel` está instalado").** Inspección directa en `paccaA100` real: `rpm -qf .../include/cblas.h` responde "no está poseído por ningún paquete" -- **no es un RPM `openblas-devel` del sistema**, es un módulo OpenHPC autocontenido en `/opt/ohpc/pub/libs/gnu12/openblas/0.3.21/`, instalado directo al árbol de módulos, al margen del gestor de paquetes de Rocky Linux. `include/` tiene el set completo esperado para compilar contra la API C: `cblas.h`, `f77blas.h`, `lapack.h`, `lapacke.h`, `lapacke_config.h`, `lapacke_mangling.h`, `lapacke_utils.h`, `openblas_config.h` (no solo Fortran/`f77blas.h`, también los headers C de BLAS y LAPACK). `lib/` tiene los symlinks de desarrollo estándar (`libopenblas.so → libopenblasp-r0.3.21.so`) y, contra lo que decía la auditoría original ("sin `pkg-config` disponible"), **sí existe un `openblas.pc` válido** en `lib/pkgconfig/` (`Cflags: -I${includedir}`, `Libs: -L${libdir} -lopenblas`) -- el módulo simplemente no agrega ese directorio a `PKG_CONFIG_PATH`, así que `pkg-config --cflags openblas` fallaba por eso, no porque el `.pc` no existiera. `$OPENBLAS_INC`/`$OPENBLAS_LIB` (las variables que `dgemm_bench` ya usa en producción desde ARC-53) apuntan exactamente a los mismos directorios que ese `.pc` describe -- confirma que la ruta de compilación real usada hoy no dependía de una casualidad, apuntaba a una instalación completa y correcta desde el principio. No requirió ningún cambio de código (DGEMM ya compilaba y corría bien) -- es cierre de una pregunta de auditoría pendiente, no una corrección. | docs/retoma/pacca/Auditoria\_PaccaA100\_Unicartagena.md (secciones 10 y 11), ARC-53 (primera compilación real de DGEMM). | **Confirmado en hardware real.** Sin acción de código pendiente; brecha cerrada. |
| ARC-58 (2026-08-06) | **`cmd_run_campaign` (CLI) ahora invoca `run_campaign_preflight()` automáticamente antes de correr la campaña -- cierra la brecha (3) documentada desde ARC-45 (2026-08-01): el preflight se venía corriendo a mano por separado (`scripts/felix/run_preflight_check.py`, `scripts/pacca/run_preflight_check.py`) y nada impedía lanzar `run-campaign` sin haberlo corrido primero.** `cmd_run_campaign` (`cli.py`) construye ahora el `node_profile` (mismo mecanismo que ya usaba `cmd_calibrate`) antes de tocar sysfs/perf/calibración, corre `preflight.run_campaign_preflight(manifest, env, catalog, node_profile=profile)`, y si hay al menos una falla bloqueante imprime el listado (`factor_id`, `name`, `message`) en JSON y retorna código de salida 1 sin invocar `campaign_module.run_campaign()` -- ninguna corrida real ni escritura de sysfs ocurre en ese camino. Sin fallas bloqueantes, el flujo sigue exactamente igual que antes. Cambio puramente local, sin necesidad de hardware real para verificarlo (la lógica de cada check individual ya estaba probada; esto solo conecta el punto de entrada) -- verificado con dos tests nuevos en `test_cli.py` (`test_run_campaign_pasa_los_argumentos_correctos` actualizado para mockear `node_profile_module.build_node_profile`/`preflight_module.run_campaign_preflight`; `test_run_campaign_arc45_corre_preflight_automaticamente_y_bloquea_si_falla` nuevo, confirma que una falla bloqueante detiene la campaña con código 1 y que `campaign_module.run_campaign` nunca se invoca). Suite completa: 205/205 tests Python en verde (204 previos + 1 nuevo). Los scripts manuales de `scripts/{felix,pacca}/run_preflight_check.py` no se tocaron -- siguen siendo útiles para inspeccionar el detalle completo de los 46 checks fuera de una campaña real. | cli.py (cmd_run_campaign), tests/orchestrator/test_cli.py, ARC-45 (hallazgo original). | **Corregido y verificado con tests.** Pendiente de una corrida real en pacca que confirme el camino feliz (preflight en verde -> campaña corre igual que antes) end-to-end, no solo con mocks. |
| ARC-60 (2026-08-06) | **Repetición de F3.4 (ARC-33) en pacca sin depender de uncore (bloqueado, ARC-59): el sesgo de `bytes_moved_window` en STREAM es ~5%, no ~34% como en felix — y es fuertemente dependiente del kernel, no una constante universal.** Con uncore cerrado, se usó el mismo mecanismo PID+inherit que ya usa el harness real (confirmado accesible bajo `paranoid=2`) para correr un probe standalone (mismo patrón que la prueba de acceso de ARC-59, no se tocó el harness C++ de producción -- ver nota de alcance abajo) que mide, sobre `stream_c` pinneado a 6 cores (mismo esquema que F4.2): el contador genérico `cache-misses` (la proxy real que usa `bytes_moved_window` hoy) y dos eventos crudos de **núcleo** (no uncore, por eso accesibles): `L2_LINES_IN_ALL` (`event=0xF1,umask=0x1F` -- todas las líneas que entran a L2, incluidas las que trae el prefetcher de hardware, exactamente el mecanismo que ARC-33 propuso como causa del sesgo) y `OFFCORE_REQUESTS_L3_MISS_DEMAND_DATA_RD` (`event=0xB0,umask=0x10` -- cota inferior, solo lecturas demand que sí llegan a memoria). Encodings confirmados con `likwid-perfctr -e` en el nodo real antes de usarlos, misma disciplina que ARC-52 (nunca adivinar codificaciones crudas de memoria). Bug propio encontrado y corregido en el camino: la primera versión del probe daba `EACCES` en los 4 contadores, incluido el genérico `instructions` que ARC-59/la auditoría original ya habían confirmado que funciona -- causa: no se seteó `exclude_kernel=1` explícito (bajo `paranoid=2` un usuario sin privilegios *debe* excluir el espacio de kernel del perfilado, si no perf\_event\_open falla incluso para un contador por-PID válido). **Resultado STREAM (3 repeticiones, `cache_misses` con CV<0.01%):** `cache_misses×64=48.47 GB` vs `51.2 GB` teórico (misma fórmula de `stream.c` que ARC-33: NTIMES=10 × 10 arreglos-toque × 64\_000\_000 elementos × 8 bytes -- STREAM\_ARRAY\_SIZE idéntico al de felix) → **déficit de 5.3%**, dentro del criterio ±30% de F3.4 con margen amplio (a diferencia de felix, que quedó fuera por poco). `L2_LINES_IN_ALL×64=48.85 GB` (déficit 4.6%) -- casi idéntico a `cache_misses`, es decir en Ice Lake-SP el prefetcher de hardware no está enmascarando tráfico de forma significativa para un patrón tan regular como STREAM, a diferencia de la hipótesis validada en Nehalem-EX/felix. **Extendido a otros kernels del catálogo** (sin fórmula teórica cerrada para NPB, así que se usa la brecha relativa `L2_LINES_IN_ALL` vs `cache_misses` como indicador, no como sesgo absoluto en bytes): `npb_cg` 10.8%, `npb_bt` 13.3%, `npb_sp` 13.5%, **`npb_mg` 31.0%** (reproducido en 3 mediciones, `cache_misses` con CV<0.2%) -- **`npb_mg` muestra una brecha comparable en magnitud a la de STREAM/felix, mientras que STREAM en pacca casi no muestra brecha** -- confirma directamente la hipótesis que ARC-33 dejó sin probar ("el sesgo puede diferir para kernels con patrones de acceso menos regulares"): no es un problema de STREAM ni de felix específicamente, es que el patrón de acceso de cada kernel determina cuánto tráfico esconde el prefetcher del contador de LLC-miss, y varía por hardware y por kernel simultáneamente. **Alcance y limitación explícita:** esto no es ground truth absoluto de bytes de DRAM (eso seguiría requiriendo uncore, ARC-59) -- la cifra de STREAM sí es sólida (contra la fórmula analítica real, no contra otro proxy), pero las cifras de CG/BT/SP/MG son solo la brecha *relativa* entre dos proxies de núcleo, útil para priorizar dónde el sesgo probablemente importa más (MG), no para corregir `operational_intensity` numéricamente todavía. No se modificó `postprocess.py` ni `bytes_moved_window` en este cambio -- es caracterización empírica, no una corrección de código; decidir si vale la pena instrumentar `L2_LINES_IN_ALL` como una métrica más en el harness real (para no depender de un probe externo) queda como decisión de diseño pendiente. Archivos de prueba (`f34_pacca_probe.c`/binario) vivieron solo en `$HOME` del nodo y se borraron al terminar, no se comprometieron al repo. | docs/retoma/pacca/Auditoria\_PaccaA100\_Unicartagena.md, docs/retoma/pacca/Solicitud\_Permisos\_Pacca\_Unicartagena.md, memoria `pacca-cluster-unicartagena-facts`, ARC-33, ARC-59, F3.4. | **Medido y verificado en hardware real** (STREAM contra fórmula analítica, 3 repeticiones; CG/MG/BT/SP contra brecha relativa entre proxies, con repeticiones en CG/MG). Hallazgo favorable para pacca como nodo final: el sesgo que preocupaba en felix es mucho menor ahí en el kernel de calibración de referencia (STREAM), aunque persiste con fuerza comparable en `npb_mg`. |
| ARC-59 (2026-08-06) | **Cierra la brecha F3.4/ARC-33 (validación cruzada de `bytes_moved_window` contra un ground truth de bytes de memoria real): confirmado que el acceso a contadores uncore/IMC está bloqueado en pacca para un usuario sin privilegios, exactamente igual que en felix (P4).** No se pudo repetir la validación con STREAM ni extenderla a otros kernels (CG/MG/etc.) en pacca porque el prerequisito (leer un PMU uncore) falla antes de llegar a medir nada. Probado por dos vías independientes en `paccaA100` real: (1) `perf_event_open` directo (`type=55` de `uncore_imc_0`, `cas_count_read`/`cas_count_write`, y el evento free-running equivalente `type=67`) da `EACCES` (errno 13) tanto en modo normal como free-running; (2) LIKWID 5.2.2 (vía su propio `likwid-accessD`, sin necesitar el CLI `perf`) llega más lejos pero el daemon -- que **no está desplegado con setuid/root en este cluster** -- falla al leer los MSR de las cajas MBOX (`failed to read/write register`); la tabla `-g MEM` en modo normal muestra números con apariencia plausible en las filas crudas (`CAS_COUNT_RD`/`WR`), pero es ruido de un registro que nunca se armó -- la tabla de métricas agregada final confirma `Memory data volume [GBytes]=0` sin ambigüedad. Causa: los eventos uncore son de alcance sistema/socket (`pid=-1`), la misma categoría que `perf_event_paranoid` restringe a partir de nivel 1 salvo `CAP_PERFMON`/`CAP_SYS_ADMIN` -- aquí `paranoid=2`, sin la capability otorgada. No hay ruta de solo-código para esto: requiere que el administrador de HPC Unicartagena baje `perf_event_paranoid`, otorgue `CAP_PERFMON` a un binario concreto, o despliegue `likwid-accessD` con setuid -- ninguna de las tres se pidió todavía (no se agregó a `Solicitud_Permisos_Pacca_Unicartagena.md` en este cambio, queda como decisión pendiente del usuario dado que no es tan urgente como H1/escritura de frecuencia). El sesgo de -30/34% medido en felix (ARC-33, solo STREAM) sigue sin cuantificar en pacca ni en ningún otro kernel del catálogo -- este hallazgo no lo resuelve, confirma que la vía prevista para resolverlo está cerrada sin intervención externa. Los archivos de prueba (`test_perf_uncore.c`/binario) se corrieron directo en `$HOME` del nodo y se borraron al terminar, no se comprometieron al repo (mismo patrón que la prueba PID+inherit original de la auditoría). | docs/retoma/pacca/Auditoria\_PaccaA100\_Unicartagena.md (sección 6, ítem 3 de bloqueadores), ARC-33, F3.4. | **Diagnosticado y verificado en hardware real** (dos vías independientes, ambas bloqueadas). Sin resolver: el sesgo de `bytes_moved_window` sigue sin poder cuantificarse de forma independiente en ningún nodo del proyecto hasta que se resuelva el permiso o se encuentre una vía alternativa que no dependa de uncore. |
| ARC-57 (2026-08-06) | **`npb_ep` y `npb_is` se quitaron del catálogo -- ninguno de los dos hace punto flotante real, comparar su "Mop/s total" contra `i_ridge` (calibrado con FLOPs reales de `ert_probe`) era un error de unidades, no un problema de medición.** A pedido del usuario tras revisar por qué `npb_is` clasificaba de forma errática (61-78% según la corrida, nunca estable) y por qué `npb_ep` casi no aportaba ventanas válidas (87-99% `intensity_undefined` en pacca y felix por igual). Diagnóstico confirmado leyendo el stdout real de cada kernel: `npb_ep` imprime `Operation type = Random numbers generated` (Monte Carlo, no FLOPs); `npb_is` imprime `Operation type = keys ranked` (bucket sort entero, no FLOPs) -- de los 6 kernels NPB del catálogo, estos son los únicos 2 que no reportan `Operation type = floating point`. En `npb_ep` el error de unidades quedaba mayormente enmascarado porque además genera casi nulo tráfico de memoria (`delta_cache_misses` con mediana 0 en ambos nodos), así que la mayoría de sus ventanas ya salían `intensity_undefined` antes de que la unidad incorrecta importara; en `npb_is` sí importaba porque su relación operaciones/bytes cae genuinamente cerca del ridge point, así que la unidad equivocada volteaba la clasificación de forma inestable corrida a corrida. Reemplazo: se compiló y verificó empíricamente (no se asumió de la documentación de NPB) el `Operation type` real de los candidatos disponibles en el mismo tarball NPB3.4.4 ya descargado (`BT`, `SP`, `UA`, `DC`) -- `npb_bt` y `npb_sp` confirmaron `Operation type = floating point`; `npb_ua` se descartó por reportar `Operation type = coll. point advanced` (tampoco FLOPs, mismo problema); `npb_dc` no llegó a producir salida útil dentro del timeout de la prueba, no se investigó más porque SP ya alcanzaba. `catalog.yaml`: `npb_ep`→`npb_bt`, `npb_is`→`npb_sp`, ambos con `phase_label_hint: intermedio` (deliberadamente sin asumir compute u memory-bound de la literatura NPB, dejando que la medición real lo confirme), checksums/tiempos/memoria medidos empíricamente en pacca (solo `pacca-a100` en el diccionario de checksums, no hay binario de felix para estos dos kernels nuevos). `campaign_pacca_ref.yaml` actualizado para usar los nuevos ids. **Verificado con una campaña real completa (21/21 aceptadas):** `npb_bt` y `npb_sp` dan **0% `intensity_undefined`** (73 613 y 50 483 ventanas `ok` respectivamente, contra las miles de ventanas descartadas que dejaba `npb_ep`); `npb_bt` clasifica 85.5% `compute_bound`/14.5% `memory_bound` (mezcla real, coherente con las fases de su método ADI); `npb_sp` clasifica 57.9% `memory_bound`/42.1% `compute_bound` (casi 50/50, exactamente lo esperable de un kernel `intermedio`); el resto de kernels se mantiene estable (`npb_mg` 99.7%, `npb_cg` 92.3%, `dgemm_n2048` 96.9%); `calibration_references.cv_pct` bajó a **0.50%**, el valor más estable de toda la sesión. | orchestrator/schemas/kernels/catalog.yaml (npb_ep/npb_is eliminados, npb_bt/npb_sp agregados), orchestrator/schemas/campaign_pacca_ref.yaml, ARC-45 (hallazgo original de npb_ep en felix), ARC-46. | **Corregido y verificado en hardware real** con una campaña completa. El dataset de 7 kernels queda con mejor cobertura física que antes (ya no hay un kernel que aporta casi nada de señal ni uno que clasifica de forma inestable). |
| ARC-56 (2026-08-06) | **`power_w` tiene picos físicamente imposibles (hasta 106 440 W) en la primera campaña real con RAPL funcionando de punta a punta (ARC-54 verificado en hardware: `energy_valid=49241/49283=99.9%`, la plomería en sí funciona).** No es un bug del fix de wiring -- es un artefacto de atribución: las ventanas con `power_w` absurdo tienen `delta_t_ns` anormalmente chico (33 000-95 000 ns, muy por debajo del intervalo nominal de 1 ms) emparejadas contra un `pkg_delta_uj` (1.7-4.3 millones de µJ) que corresponde a un intervalo de energía mucho más largo que esa ventana de CPU puntual -- `postprocess._match_energy_windows()` le asigna a cada ventana de CPU la fila ENERGY cuyo propio delta "mejor la representa" por timestamp, pero si el muestreo de RAPL tiene una cadencia distinta a la de CPU (1 ms), un delta de energía grande puede terminar dividido entre un `delta_t_ns` de una ventana de CPU inusualmente corta, inflando `power_w` sin límite. Consistente con esto: la mediana global de `power_w` es exactamente 0.0 (la mayoría de las ventanas de CPU no tienen una lectura RAPL nueva dentro de su intervalo tan corto, así que su `pkg_delta_uj` emparejado es 0), mientras que unas pocas ventanas acumulan el delta de un intervalo RAPL más largo. La media agregada (164.47 W) es físicamente plausible para este CPU bajo carga, así que las cifras *agregadas* probablemente sean utilizables como primera aproximación, pero **ninguna lectura de `power_w` por ventana individual debe tomarse como confiable todavía** sin corregir el emparejamiento (posible fix: usar el delta_t_ns de la propia ventana ENERGY para la potencia, no el delta_t_ns de la ventana CPU emparejada; o exigir que la ventana ENERGY esté completamente contenida en la ventana CPU antes de calcular power_w). **Corregido a pedido explícito del usuario junto con ARC-55.** `_match_energy_windows()` ahora devuelve, junto con la fila ENERGY emparejada, el intervalo REAL que su propio `pkg_delta_uj` abarca (el timestamp de esa fila menos el de la fila ENERGY inmediatamente anterior en la lista ordenada -- exactamente el par de muestras RAPL consecutivas que produjeron ese delta). `power_w` se calcula con ese intervalo, nunca con `delta_t_ns` de la ventana CPU emparejada. Sin un intervalo propio válido (p.ej. la primera lectura RAPL de la corrida), `power_w` queda `None` en vez de usar un denominador que no le corresponde. Test de regresión nuevo (`test_arc56_power_w_usa_el_intervalo_real_de_rapl_no_el_de_la_ventana_cpu`) reproduce el escenario exacto (ventana CPU de 10 ns emparejada contra un delta RAPL de 1 ms real) y confirma `power_w=2000W` en vez de los ~2×10⁸ W que daba el cálculo roto. **Hallazgo adicional al verificar en hardware real:** la corrida que reveló ARC-56 nunca tuvo ventanas de CPU anómalamente cortas después de que ARC-55 (afinidad real) se corrigió -- la contención de 32 hilos sin pinnear era la causa raíz del jitter de muestreo que producía esas ventanas de 33-95 µs en primer lugar. Con ARC-55 corregido, `power_w` sale sano (máx. 321-351 W, mediana ~117 W, físicamente coherente) incluso con el código *anterior* a este fix -- confirmado corriendo una campaña completa con solo ARC-55 aplicado antes de sincronizar este fix. Este fix de `postprocess.py` sigue siendo correcto y necesario como defensa en profundidad (el desajuste de cadencia CPU/RAPL puede reaparecer bajo otras condiciones de jitter), no fue una corrección innecesaria. | postprocess.py (_match_energy_windows, cálculo de power_w), test_postprocess.py, ARC-54, ARC-55. | **Corregido y verificado en hardware real** (campaña completa con ambos fixes activos, `power_w` máx. 351 W, mediana 117 W, `energy_valid=99.96%`). |
| ARC-55 (2026-08-06) | **`cores.delegated_cpus` está declarado en cada manifiesto pero nunca se aplica como afinidad de CPU real al proceso medido — ni siquiera `OMP_NUM_THREADS`.** Encontrado durante la primera campaña real en pacca (ARC-53/54): `runner.py::build_command()` solo traduce `cores.delegated_cpus` a `--perf-cpus` (qué CPUs escucha `perf_event_open`, no dónde corre el proceso) y `subprocess.Popen` (runner.py) no fija ni `env=` con `OMP_NUM_THREADS` ni `sched_setaffinity` sobre el hijo — el proceso medido hereda el entorno del orquestador tal cual y el kernel/OpenMP eligen threads y CPUs libremente. Confirmado empíricamente: `ert_probe` corrido sin fijar hilos reporta `OPENMP_THREADS 32` (el nodo completo, no los 6 cores declarados en `cores.delegated_cpus: [0,1,2,3,4,5]`), y ese valor de 32 hilos es el que coincide con lo que la calibración real de la campaña midió (`P_pico` observado ≈ lo que da `ert_probe` con 32 hilos, no con 6). La atribución de contadores por PID (`perf_event_open` con `inherit=1`) sigue siendo correcta pase lo que pase (mide exactamente el árbol de procesos del kernel medido, no CPUs físicas) — este hallazgo no invalida la atribución de eventos, pero sí significa que "6 cores delegados" nunca fue una restricción real de recursos para el workload medido, solo una etiqueta usada para calcular el dominio de frecuencia (E10) y el pinning de `collector_cpu`/`consumer_cpu`. Esto aplica retroactivamente a **toda corrida real del proyecto hasta ahora, incluida la campaña de felix ya reportada** (`Informe_Diagnostico_Beta_2026-08-05.md`) — no se sabe sin volver a revisar si eso cambia alguna conclusión ahí (probablemente no cambia la clasificación relativa compute/memory-bound per-kernel, que depende de la posición relativa en el espacio IPC/intensidad medido por-proceso, pero sí puede afectar cualquier comparación absoluta de rendimiento entre corridas asumiendo "6 cores"). **Corregido a pedido explícito del usuario ("busquemos solución a todas esas incoherencias").** El launcher C++ ya tenía el mecanismo real (`--pin-workload-cpus`, `set_affinity()` sobre el hijo antes de `execv`, usado manualmente en la validación F3.4 de felix pero nunca conectado a la ruta de producción) — solo faltaba invocarlo desde `runner.py`. Fix de dos partes: `build_command()` agrega `--pin-workload-cpus <delegated_cpus>` siempre; `run_single()` pasa `env=` a `subprocess.Popen` con `OMP_NUM_THREADS=len(delegated_cpus)` (heredando el resto del entorno, necesario para `LD_LIBRARY_PATH` de OpenBLAS). Ninguno de los dos solo no alcanzaba: la afinidad sin fijar hilos permite oversubscription (32 hilos compitiendo por 6 cores); fijar hilos sin afinidad no impide que el scheduler los mueva fuera de los cores delegados. **Verificado en dos campañas reales completas consecutivas en pacca (21/21 aceptadas cada una, resultados reproducibles):** `calibration_references.cv_pct` bajó de 4.57-8.46% (inestable) a 2.24-2.75% (estable); `npb_cg` pasó de clasificar mal (75-89% `compute_bound`, el hint dice `memory_bound`) a **92.7% `memory_bound` correcto**; `npb_mg` pasó de inestable (66-78%) a **99.6-99.8% `memory_bound` correcto** -- ambos coinciden ahora con lo que ya se había visto en felix. `npb_is` sigue sin clasificar bien (61% `compute_bound` cuando el hint dice `memory_bound`) -- a diferencia de `npb_cg`/`npb_mg` esto no mejoró con el fix, así que podría ser un caso genuinamente borderline en este hardware más moderno (Integer Sort tiene intensidad aritmética baja pero también tráfico de memoria absoluto bajo), no atribuible a este bug -- queda como observación abierta, no como parte de ARC-55. Tests: `tests/orchestrator/fixtures/fake_launcher.py` actualizado para aceptar `--pin-workload-cpus` (el fake launcher usado en `test_runner.py` no lo reconocía, exit code 2). 204/204 tests Python en verde (192 anteriores + 11 de `test_runner.py`, corridos por primera vez esta sesión tras instalar `psutil` localmente, + 1 nuevo de `test_postprocess.py` para ARC-56). Bug propio encontrado y corregido en el camino: el primer intento de este fix accedía a `manifest.rapl` directo en `build_command()`, rompiendo los tests que usan un manifiesto `SimpleNamespace` sin ese campo -- corregido con `getattr(manifest, "rapl", {})`. **Pendiente, fuera de alcance de esta corrección:** no se volvió a correr la campaña ya reportada de felix con este fix, así que sus conclusiones publicadas (`Informe_Diagnostico_Beta_2026-08-05.md`) siguen sin confirmar bajo afinidad real -- felix está descartado como nodo final así que no se considera prioritario. | runner.py (build_command, run_single), tests/orchestrator/fixtures/fake_launcher.py, orchestrator/schemas/campaign_pacca_ref.yaml (hardware_datasheet recalibrado a 6 cores reales). | **Corregido y verificado en hardware real con dos campañas completas reproducibles.** |
| ARC-54 (2026-08-06) | **`manifest.rapl.enabled` nunca se traducía en los flags `--rapl-pkg`/`--rapl-dram` que el harness C++ necesita para abrir RAPL — la captura de energía nunca funcionó de punta a punta en ninguna campaña real del proyecto.** Invisible en felix (sin RAPL físicamente, así que nadie notó la ausencia). Encontrado en la primera campaña real de pacca (ARC-53): con `rapl.enabled: true` y RAPL confirmado legible en auditorías previas, `windows.csv` mostró `energy_valid=0` en las 40 118 ventanas de las 21 corridas -- `metadata.json` confirmó `rapl_energy_delta_count: 0` en cada corrida. Causa raíz: `runner.py::build_command()` nunca leía `manifest.rapl` en absoluto (`grep rapl runner.py` no devolvía nada antes de este fix); el launcher C++ sí soporta `--rapl-pkg`/`--rapl-dram` (`telemetry_kernel_launcher.cpp`, ya usados manualmente en `scripts/felix/run_f34_validation.sh` para una validación puntual, nunca por la ruta de producción `campaign.py`/`runner.py`). Corregido: `build_command()` gana un parámetro `environment_profile` (ya lo tenía disponible el único call site real, `campaign.py:282`, solo faltaba pasarlo); cuando `rapl.enabled` es `true`, resuelve la ruta del dominio correspondiente al socket pineado (`cores.numa_node_pin`) buscando los alias `package-{numa_node}`/`dram-package-{numa_node}` en `environment_profile.rapl_domain_paths` (ya derivados por `environment.py` del archivo `name` real de sysfs, ver `_rapl_data()`) -- si no hay coincidencia exacta para ese NUMA node, RAPL simplemente no se activa para esa corrida, nunca asume una ruta genérica ni el primer dominio disponible. Verificado con un dry-run real en pacca (`runner.build_command()` con el manifiesto real): produce `--rapl-pkg /sys/class/powercap/intel-rapl/intel-rapl:0 --rapl-dram .../intel-rapl:0:0`, coincide exactamente con `package-0`/`dram-package-0` confirmados en la auditoría. 192/192 tests Python en verde (los tests existentes de `build_command` no pasan `environment_profile`, que por default es `None` y omite el bloque RAPL -- retrocompatible sin cambios). **Confirmado en una campaña real completa (21/21 aceptadas): `energy_valid=49241/49283=99.9%`** -- la plomería RAPL funciona de punta a punta en hardware real por primera vez en el proyecto. La media agregada de potencia (164.47 W) es físicamente plausible para este CPU bajo carga. Un problema *distinto* de atribución por ventana individual (picos de `power_w` de hasta 106 440 W) se encontró en la misma corrida -- ver ARC-56, no es un fallo de este fix sino del emparejamiento posterior CPU↔ENERGY en `postprocess.py`. | runner.py (build_command, run_single), environment.py (rapl_domain_paths, ya existía), campaign.py (ya pasaba environment_profile, sin cambios). | Corregido y verificado en hardware real con una campaña completa. Primera captura de energía real de punta a punta en el proyecto. |
| ARC-53 (2026-08-06) | **F4.1/F4.2-equivalente para pacca: primer setup real de punta a punta, preflight completo (46 checks) en verde por primera vez en este nodo.** Repo clonado en `~/hyperion` (HTTPS anónimo, el proyecto es público — sin necesidad de deploy key), convención `hyperion-kernels/`/`hyperion-results/` replicada, venv Python 3.11 propio (`~/hyperion-venv`, conda del sistema sin canales configurados así que se usó `venv`+pip en vez de crear un env conda). Catálogo completo compilado en pacca: NPB×6 (clase B, checksums verificados contra fuente oficial NASA), DGEMM (OpenBLAS vía módulo, sin `pkg-config` disponible — se usó `OPENBLAS_INC`/`OPENBLAS_LIB` del entorno del módulo directamente), STREAM/ERT. Encontrados y corregidos dos bloqueadores reales antes de llegar a preflight verde: (1) **CMake elegía `nvc++` (NVIDIA HPC SDK) en vez de `g++` del sistema** para el harness C++ -- el entorno "AdaptiveCpp" que carga pacca por defecto expone `nvc++` como compilador C++ activo, causando un choque de ABI de `libstdc++` (`GLIBCXX_3.4.26' not found`) en 2 de 10 tests; corregido forzando `-DCMAKE_CXX_COMPILER=g++` explícito, 10/10 tests en verde con hardware real (incluye `rapl_reader_test` y `perf_reader_pid_live_test` con contadores creciendo en vivo). (2) **`binary_checksum` en `catalog.yaml` era un único valor global** calculado una vez en felix (gnu14/14.2.0) -- el mismo código fuente compilado con gcc 12.4.0 en pacca produce binarios funcionalmente idénticos pero con hash SHA-256 distinto, bloqueando C02 en las 9 entradas del catálogo. Se generalizó `binary_checksum` a `str \| dict[node_id, checksum]` (`catalog.py`), `verify_binary()` ahora recibe `node_id` y falla cerrado si el checksum es un dict sin entrada para ese nodo (mismo principio que D05: nunca aprobar por default) -- hilado a través de `preflight.check_binary_checksum`/`run_campaign_preflight` (via `node_profile.node_id`), `runner.run_single` (ya tenía `node_id` como parámetro, solo faltaba pasarlo), `validation.validate_run` (parámetro nuevo) y `campaign.py` (pasa `node_id` a `validate_run`). `catalog.yaml` actualizado con checksums reales de ambos nodos (`felix-sc3`/`pacca-a100`) en las 9 entradas, generado por script (no a mano, para eliminar riesgo de transcripción en un hash). Tercer bloqueador, D05 (`pmc_count=0` porque `probe_pmc_count()` depende del CLI `perf`, ausente en pacca): en vez de reimplementar el struct `perf_event_attr` en Python/ctypes (mismo riesgo de ABI mal replicado que ya mordió en ARC-52), se agregó `orchestrator/native/pmc_multiplex_probe.c`, un helper C que reproduce el mismo criterio de multiplexado con el mecanismo `perf_event_open` ya validado en `telemetry/src/perf_reader.cpp`; `environment._run_perf_stat_probe()` cae a compilarlo y correrlo (cacheado en `/tmp`) solo cuando `perf` no está, devolviendo el mismo formato de marcador que `_MULTIPLEXING_MARKER` ya reconoce -- `probe_pmc_count()` no cambió. Resultado en hardware real: `pmc_count=10` (los 10 eventos genéricos abren simultáneos sin multiplexar en Ice Lake, más presupuesto que los 5 de felix/Nehalem-EX). Con los tres corregidos: **`scripts/pacca/run_preflight_check.py` (adaptado de `scripts/felix/`, RAPL habilitado a diferencia de felix) da 46/46 checks en verde, 0 fallas bloqueantes.** Suite completa de tests Python (192) sigue en verde tras los cambios. Aún sin correr una campaña real (`run-campaign`) contra pacca -- eso es el siguiente paso natural, a un solo nivel de frecuencia (REF) porque H-equivalente (escritura de cpufreq) sigue sin respuesta del administrador (ver `Solicitud_Permisos_Pacca_Unicartagena.md`). | catalog.py, preflight.py, runner.py, validation.py, campaign.py, catalog.yaml, environment.py (native probe fallback), orchestrator/native/pmc_multiplex_probe.c, scripts/pacca/run_preflight_check.py, ARC-50, ARC-51, ARC-52. | Implementado y verificado en hardware real (paccaA100): preflight completo en verde por primera vez en este nodo. Campaña real todavía pendiente. |
| ARC-52 (2026-08-06) | **Los stall cycles SÍ se miden en pacca, siguiendo el plan original** (ARC-51 los dejaba correctamente degradados pero ausentes; el usuario pidió explícitamente no conformarse con la degradación — "en el plan dijimos que los ibamos a usar, debemos usarlos"). Diagnóstico exacto: `PERF_COUNT_HW_STALLED_CYCLES_BACKEND` da `ENOENT` en pacca porque la tabla de traducción evento-genérico→evento-crudo del kernel (RHEL8 backport) no tiene entrada para Ice Lake-SP (family=6, model=106, confirmado leyendo `/proc/cpuinfo`), pero el contador crudo subyacente sí existe en el hardware. Primer intento de workaround (`perf_event_open` con `PERF_TYPE_RAW`, config=evento 0xA3 + umask 0x04, la codificación "de memoria" de `CYCLE_ACTIVITY.STALLS_TOTAL` para la familia Skylake) **abrió sin error pero devolvió un valor físicamente imposible** (`stalls_total=2.56B > cycles=1.92B`, ratio 133%) — confirma que adivinar codificaciones crudas de memoria es peligroso: un valor con apariencia válida puede estar midiendo otra cosa, mucho peor que un `ENOENT` limpio. Se buscó la codificación autoritativa en la tabla de eventos validada de LIKWID (`likwid-perfctr -e \| grep -i stall` en pacca, sin depender del acceso a MSR que sigue bloqueado — solo se leyó el catálogo de eventos, no se ejecutó nada que necesite `likwid-accessD`): `CYCLE_ACTIVITY_STALLS_TOTAL, 0xA3, 0x4, PMC, THRESHOLD=0x4` — el campo `THRESHOLD` es el CMask (bits 24-31 del config crudo de x86), que el primer intento omitió por completo. Con `config = 0xA3 \| (0x04<<8) \| (0x04<<24) = 0x040004A3` la prueba se repitió y dio un resultado plausible (`stalls=640M`, `cycles=1.93B`, ratio 33.2%, `0<ratio<1`). Implementado en `perf_reader.cpp`: `detect_intel_family_model()` lee `/proc/cpuinfo` en tiempo de ejecución (falla cerrado -- nunca aplica el fallback si no pudo identificar la CPU con certeza); cuando el evento genérico del índice `kStalledCyclesBackend` falla Y la CPU es exactamente family=6/model=106, se reintenta una sola vez con `PERF_TYPE_RAW` y la codificación validada; si ese modelo no coincide (cualquier otra CPU, incluida felix) el comportamiento de degradación de ARC-51 sigue intacto sin cambios. Verificado con la clase real `telemetry::PerfReader` (no un syscall suelto) compilada y corrida en paccaA100: `has_stalled_cycles_backend()=true`, `stalled_cycles_backend=605.332.321` de `cycles=1.812.920.563` (ratio ~33.4%, consistente con la prueba anterior). Build C++ limpio, 10/10 tests locales en verde; Python 192/192 en verde (el fallback es transparente para `postprocess.py`, no requirió cambios ahí). Con esto, en pacca `stall_backend_ratio` en `windows.csv` va a tener datos reales, no `None` — cumple el compromiso del plan original de usar stall cycles para discriminar fases memory-bound (ARC-27), en vez de quedarse solo con la degradación elegante de ARC-51. | perf_reader.cpp (detect_intel_family_model, make_raw_attr, kIceLakeSPFamily/Model, kIceLakeStallsTotalRawConfig), ARC-27, ARC-50, ARC-51. | Implementado y verificado en hardware real (paccaA100) con la clase real del harness, no solo una prueba aislada. Gateado estrictamente a family=6/model=106 -- no se ha intentado ni se debe asumir en ningún otro hardware sin repetir esta misma validación. |
| ARC-51 (2026-08-05) | **Corrección crítica a ARC-50, verificada en hardware real antes de que el bug llegara a producción.** Prueba empírica en paccaA100 (programa C mínimo con el mismo mecanismo PID+inherit del harness, sin depender del CLI `perf` que no está instalado ahí): `PERF_COUNT_HW_STALLED_CYCLES_BACKEND` y `-FRONTEND` fallan con `errno=2 (No such file or directory)` — el kernel de pacca (RHEL8, `4.18.0-553.109.1.el8_10`) no tiene mapeado ese evento genérico para Ice Lake, mientras que los otros 4 contadores (`instructions`, `cycles`, `cache_references`, `cache_misses`) abren sin problema con `running_pct=100%`. No es un problema de permisos, es una brecha de la tabla de eventos del kernel/PMU — y como `PerfReader::open()` (tal como quedó en ARC-50) lanza excepción y aborta TODO si falla un solo evento, `telemetry_kernel_launcher` se hubiera caído al arrancar en pacca, no solo perdido el dato nuevo. Nunca se había probado en hardware real (solo build+tests locales en una máquina sin ese problema). Corregido con degradación elegante: `kCoreEventCount=4` son obligatorios (fallar aborta, comportamiento sin cambios), el 5º evento es best-effort (si falla, `fds_[kStalledCyclesBackend]=-1`, no se arma ni se lee, el reader sigue funcionando con los 4 de siempre). Nuevo `PerfReader::has_stalled_cycles_backend()` expone la disponibilidad; `Collector` lo reexpone; debe leerse ANTES de `collector.stop()` (que cierra el reader y lo volvería siempre falso) — capturado justo después de `collector.start()` en `run_child()`, propagado vía un campo nuevo en `ChildResult`, y OR'd a través de las repeticiones de la corrida (es un hecho de capacidad del nodo, no debería variar entre repeticiones de la misma corrida, pero se combina defensivamente igual). `write_samples_csv()` recibe el flag y escribe la columna **vacía** (nunca `"0"`, que se leería como una medición real) en las filas CPU de toda la corrida cuando el nodo no soporta el contador. En `postprocess.py`: `stall_backend_supported` se detecta por corrida (¿algún valor no vacío en `stalled_cycles_backend` de `cpu_rows`?) — si no, `delta_stalled_cycles_backend`/`stall_backend_ratio` quedan `None` sin participar en el gate `pmu_degraded` ni contaminar `ipc`/`ips`/`mpki` del resto de contadores; si sí, se mantiene el mismo rigor que ARC-50 (contador negativo o ausente en una corrida que SÍ lo soporta sigue siendo una anomalía real). Esto también cubre gratis el caso de un `samples.csv` de antes de ARC-50 (columna ausente por completo), que antes se hubiera marcado `pmu_degraded` en el 100% de las ventanas por un `TypeError` potencial en el cálculo de `stall_backend_ratio` (`None / delta_cycles`), también corregido. Test reescrito (`test_stalled_cycles_backend_no_soportado_en_el_nodo_no_marca_pmu_degraded`) para reflejar el comportamiento correcto. Build C++ limpio, 10/10 tests en verde (esta vez incluso `perf_reader_pid_live_test` corrió, la máquina de desarrollo sí tiene permiso PMU). Python: 192/192 en verde. Pendiente: confirmar en felix que los 4 contadores core + el 5º (si abre ahí) siguen sin multiplexar; la pregunta de presupuesto de PMCs bajo SMT en pacca sigue abierta porque el 5º evento nunca llegó a competir por un slot (falló al abrir, no por falta de cupo). | perf_reader.hpp/.cpp, collector.hpp, telemetry_kernel_launcher.cpp (ChildResult, run_child, write_samples_csv), postprocess.py (stall_backend_supported), test_postprocess.py, ARC-50. | Corregido y verificado en hardware real (paccaA100) antes de llegar a una corrida de campaña. Sin este fix, ARC-50 hubiera roto el harness en el primer intento real de correr ahí. |
| ARC-50 (2026-08-05) | **El harness nunca midió stall cycles**, hallazgo del usuario durante la exploración de clústeres candidatos (pacca/Unicartagena). Confirmado: `PerfReader::open()` (`perf_reader.cpp`) abría exactamente 4 contadores fijos (`instructions`, `cycles`, `cache_references`, `cache_misses`, `kEventCount=4`); `CpuSample` (`metrics.hpp`) no tenía campo para stalls; `postprocess.py` no tenía ninguna lógica que dependiera de ellos. No era una regresión — nunca se implementó, aunque D05/ARC-37 ya había medido que felix soporta 5 contadores simultáneos sin multiplexar (uno más de los 4 usados) y ARC-27 ya había identificado que "el tiempo de reloj está confundido por los stalls de memoria — exactamente el efecto que `phase_label_train` necesita discriminar", sin que nadie cerrara ese cabo. Se agregó `PERF_COUNT_HW_STALLED_CYCLES_BACKEND` como 5º contador (elegido sobre `stalled-cycles-frontend` por ser la señal directa de espera de memoria/recursos de ejecución, la más relevante para Roofline) en `perf_reader.hpp/.cpp`, campo `stalled_cycles_backend` en `CpuSample`, columna nueva en el CSV de `telemetry_kernel_launcher.cpp` (entre `cache_misses` y `time_enabled_ns`, filas ENERGY/GPU ajustadas para mantener el CSV rectangular). En `postprocess.py`: `delta_stalled_cycles_backend` se calcula igual que los otros 3 contadores core y participa en el mismo gate `pmu_degraded` (contador negativo o ausente invalida la ventana, no se oculta); `stall_backend_ratio` (fracción de ciclos parados en el backend) se agrega como métrica derivada nueva en `REQUIRED_OUTPUT_COLUMNS`, mismo patrón que `ipc`/`mpki`/`llc_miss_rate`. No se integró (deliberadamente, fuera de alcance de este cambio) en `calibration.py` ni en la fórmula de `phase_label_train` — sigue siendo `bytes_moved_window`/`operational_intensity`; usar el nuevo campo para mejorar esa clasificación queda como decisión de diseño pendiente. Fixture `tests/orchestrator/fixtures/fake_samples.csv` actualizado con la columna nueva (antes rompía silenciosamente `test_fixture_fake_samples_...`, marcando `pmu_degraded` en todas las filas por columna faltante — detectado al correr la suite completa tras el cambio). Build de C++ limpio, 10/10 tests locales en verde (el único skip es `perf_reader_pid_live_test`, esperado sin permiso PMU en la máquina de desarrollo). Suite de Python: 192/192 en verde (190 previos + 2 nuevos: cálculo correcto del delta/ratio, y gate `pmu_degraded` cuando la columna falta — CSV de una corrida anterior al cambio). **Verificado en hardware real en ARC-51: el 5º contador falla con ENOENT en paccaA100 (kernel/PMU no lo mapea, no es un problema de permisos) — sin el fix de degradación elegante de ARC-51 esto hubiera roto el harness ahí por completo.** | perf_reader.hpp/.cpp, metrics.hpp, telemetry_kernel_launcher.cpp, postprocess.py (REQUIRED_OUTPUT_COLUMNS, build_windows, _base_row), test_postprocess.py, fake_samples.csv, D05, ARC-27, ARC-37, ARC-51. | Implementado; ver ARC-51 para la corrección de portabilidad encontrada al verificar en hardware real. |

## Flujo HPC actualizado

1. Crear/activar el entorno Conda Python 3.11 y solicitar una asignación Slurm
   con un único NUMA y `--hint=nomultithread`.
2. Ejecutar el diagnóstico de solo lectura con `--use-allowed-cpus` y conservar
   `startup_diagnostic.json`.
3. Resolver el manifest de campaña en tiempo de job usando cpuset y cgroup hijo
   reales; validar catalog y binarios antes de tocar frecuencia.
4. Ejecutar el preflight de campaña completo. Si no existe permiso de control,
   permitir únicamente una campaña explícitamente nativa/F0-only, etiquetada
   como no apta para entrenamiento DVFS.
5. Ejecutar calibración y matriz solo cuando C01/C02, RAPL, cgroup, NUMA/SMT y
   los checks de frecuencia correspondientes hayan pasado.
6. En `felix`, solicitar y verificar la GPU NVIDIA asignada por Slurm antes de
   habilitar GPU; implementar el inspector NVIDIA correspondiente.

## Criterio de cambio futuro

Cada nueva entrada añadida a este archivo debe indicar:

1. ID `ARC-NN` y fecha.
2. Motivo y evidencia observada.
3. Reglas/checklists/flujo que modifica.
4. Módulos y pruebas afectados.
5. Estado: propuesto, implementado, verificado en hardware o descartado.
